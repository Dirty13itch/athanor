---
name: port-hydra-module
description: Port a Python module from Hydra reference repo into Athanor
version: "1.0"
parameters:
  module_name:
    description: "Module to port (e.g., routellm, preference_learning)"
    required: true
---
Port ~/repos/reference/hydra/src/hydra_tools/{{ module_name }}.py into Athanor.
Read the source, adapt Hydra patterns (TabbyAPI→LiteLLM, CrewAI→LangGraph, NixOS→Ansible), write tests, lint with ruff, commit.
