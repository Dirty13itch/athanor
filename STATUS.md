# Athanor Status

**Last updated:** 2026-04-16 00:32 CDT
**Program:** Truth convergence, Ralph-loop steady-state governance, shadow-tier promotion follow-through, provider weak-lane closure, runtime recovery, aggressive prune, and low-touch devstack turnover prep

> **Boundary:** This file is a curated operator summary, not the sole live runtime oracle. For mutable queue, runtime, provider, deployment, and publication state, prefer `python scripts/session_restart_brief.py --refresh`, `reports/ralph-loop/latest.json`, `reports/truth-inventory/finish-scoreboard.json`, `reports/truth-inventory/runtime-packet-inbox.json`, `reports/truth-inventory/`, and the generated reports under `docs/operations/`. Multi-system planning and external dependency truth now start with `docs/operations/ATHANOR-ECOSYSTEM-MASTER-PLAN.md` and `docs/operations/ATHANOR-OPERATOR-MODEL.md`.

## Program Health

| Lane | Status | Notes |
|------|--------|-------|
| Authority model | Completed | `C:\Athanor` is the implementation authority, DEV runtime state is explicitly governed through the runtime-ownership packet layer, and `athanor-next` remains incubation-only. |
| Startup docs | Completed | Top-entry docs, backlog truth, operating-system truth, Ralph-loop state, and the ecosystem planning/operator surfaces now converge materially instead of routing humans and agents to stale startup layers. |
| Mainline synthesis | Completed | The clean synthesis lane landed the 2026-03-29 through 2026-03-30 Athanor-core integrator train plus the remaining script-service contract slice onto `main`, and the follow-on normalization tranche collapsed the repo to one live branch, one live remote ref, and one registered worktree. The deleted branch and worktree state is preserved under `C:\Athanor_cleanup_archive\2026-03-30-branch-normalization`, so branch sprawl is no longer active repo debt. |
| Truth inventories | Steady-state monitoring | Hardware, model, provider, subscription-burn, tooling, credential, repo-root, and routing-taxonomy registries are now canonical, validator-backed, and routinely refreshed; remaining drift is runtime-owned maintenance, not inventory ambiguity. |
| Portfolio and source reconciliation | Completed | The three-depth `C:\` sweep, GitHub portfolio mirror, tenant-family audit, and ecosystem-role classification now cover all 35 live Dirty13itch repos and the meaningful local roots. Remaining work is execution and freeze follow-through under existing packets, not classification drift. |
| Ralph-loop control | Steady-state monitoring | `completion-program-registry.json`, `scripts/run_ralph_loop_pass.py`, `reports/ralph-loop/latest.json`, and `docs/operations/ATHANOR-RALPH-LOOP-PROGRAM.md` now act as the standing execution selector. The current live posture is `steady_state`, and the live loop family is now `governor_scheduling` with `ranked_autonomous_dispatch` instead of falling back to stale evidence-refresh churn. |
| Generated reports | Steady-state monitoring | Truth inventory reports, runtime-ownership surfaces, provider evidence, and bootstrap sync artifacts now refresh cleanly and agree with the validator-backed control plane. Remaining work is ordinary evidence refresh and packet reprobe after live maintenance. |
| Operator front door | Completed | `config/automation-backbone/operator-surface-registry.json` and `docs/operations/OPERATOR-SURFACE-REPORT.md` own human-facing surface truth, the dashboard IA is regrouped into command-center/operate/build/domains/catalog, the DEV command center runs as a containerized dashboard behind Caddy at `https://athanor.local/`, WORKSHOP `:3001` is retired as a command-center surface, the live container is standardized onto `/opt/athanor/dashboard`, DESK now resolves `athanor.local` plus the node-host aliases through the scripted hosts-file rollout, and both canonical browser verification plus runtime fallback smoke are green. The topology-owned dashboard health probe uses `/api/operator/session`, so the generic service-health lane no longer reports a false red from the heavyweight `/api/overview` aggregate. |
| Runtime drift closure | In progress | The audited runtime-drift surface is narrower and clearer than before, but it is not fully green. `reports/deployment-drift/summary.md` now shows active repo-vs-runtime drift on `foundry-gpu-orchestrator`, `vault-litellm`, `vault-prometheus`, `workshop-comfyui`, `workshop-eoq`, and `workshop-ulrich-energy`, while `workshop-vllm` and `workshop-open-webui` currently read as `no_containers` at runtime even though their rendered compose roots still exist. The DEV runtime repo sync, VAULT Prometheus/LiteLLM reconcile, FOUNDRY agents deploy, FOUNDRY GPU Orchestrator deploy, and Workshop control-surface/product compose lanes remain packet-backed runtime-ownership work; the remaining closure is now live runtime follow-through plus provider-secret remediation, not hidden drift ambiguity. |
| Autonomy activation | Completed | `full_system_phase_3` is now live on the current FOUNDRY runtime, `broad_autonomy_enabled` is true, and the live roster includes the sidecar and creative lanes under registry-backed scope. Runtime mutations still remain approval-gated, provider scope still follows canonical routing posture, sovereign-only creative routing remains in force, and the runtime-ownership contract continues to govern the remaining DEV repo-sync maintenance without acting as a promotion blocker. |
| Devstack handoff and shadow promotion | Devstack primary build and shadow | Router shadow phase `1` is live, the atlas currently reports `devstack_primary_build_and_shadow`, and devstack now owns build-state queueing through packets, forge board, and atlas rather than through Athanor startup prose. Accepted and adopted deltas remain visible through packet-backed lineage and atlas truth, while the live ranked autonomous queue continues to lead with `dispatch-and-work-economy-closure`, `capacity-and-harvest-truth`, and `validation-and-publication`. The current slot-granularity capacity tranche still flows end to end through `reports/truth-inventory/capacity-telemetry.json` and `reports/truth-inventory/quota-truth.json`, so Foundry bulk harvest, Workshop batch support, and the shared DEV support hold remain visible as real scheduler-slot truth instead of a coarse node-only budget. |
| Provider truth cleanup | Steady-state monitoring | Active routing and burn lanes are now catalog-backed end to end, and the current routing contract keeps the blocked or demoted optional VAULT API lanes out of ordinary auto-routing instead of pretending they are required for expanded-core autonomy. `anthropic_api`, `deepseek_api`, `mistral_codestral_api`, `moonshot_api`, `venice_api`, and now `zai_api` are proven through live completions on the VAULT LiteLLM surface, which clears the turnover-critical provider gate. The weak non-API lanes are now also explicit instead of fuzzy: Kimi is still `live_burn_observed_cost_unverified`, GLM now has an observed supported-tool proof in `reports/truth-inventory/planned-subscription-evidence.json`, and `reports/truth-inventory/provider-usage-evidence.json` remains VAULT-proxy-only evidence rather than a catch-all lane for CLI or supported-tool subscriptions. Remaining work is optional elasticity maintenance: `openai_api`, `google_gemini_api`, `dashscope_qwen_api`, and `openrouter_api` are still explicitly demoted pending present-key rotation or auth-surface repair, while direct `moonshot_api` env closure remains optional because the live `kimi-k2.5` alias path is already observed. |
| Secret-surface remediation | Steady-state monitoring | DEV cron now sources `/home/shaun/.athanor/runtime.env` through `BASH_ENV`, the reviewed Athanor systemd estate is envfile-or-envless by contract, DESK runtime env is present, and recovery artifacts now redact credential-bearing URLs before being written. The current VAULT LiteLLM env-name audit persists under `reports/truth-inventory/vault-litellm-env-audit.json`, the platform validator requires it for the VAULT credential surface, the generated provider plus secret-surface reports surface exact missing env names instead of generic auth-repair follow-up text, and `docs/operations/VAULT-LITELLM-AUTH-REPAIR-PACKET.md` is the current operator checklist for the narrowed maintenance surface. The live container now carries `ANTHROPIC_API_KEY`, `CODESTRAL_API_KEY`, `DEEPSEEK_API_KEY`, `GEMINI_API_KEY`, `GOOGLE_API_KEY`, `MISTRAL_API_KEY`, `OPENAI_API_KEY`, `OPENROUTER_API_KEY`, `VENICE_API_KEY`, and `ZAI_API_KEY`; the remaining missing env names are `DASHSCOPE_API_KEY` and `MOONSHOT_API_KEY`. The remaining secret-surface work is optional-elasticity repair plus direct-env cleanup, not a turnover blocker. |
| Core platform follow-through | In progress | The legacy task-mutation bypass is closed, and privileged task create/approve/cancel/reject/review, manual task dispatch, scheduled-job manual run, governor heartbeat, emergency stop/resume, research job create/execute/delete, project milestone/advance/supervise, workforce goal create/delete, notification resolution, preference writes, explicit and implicit feedback, gallery feedback, event ingestion, pipeline steering, workspace mutation, workplan generation/redirect, convention propose/confirm/reject, subscription lease/execution/handoff mutation, manual consolidation/pattern/improvement trigger paths, skill create/execution/delete, core-memory update, digest generation, workflow execution, Workshop GPU swap, model-governance denied-path auditing, and diagnosis auto-remediation now use the shared operator envelope and audit contract where they mutate or shell out. The dashboard health summary now reports `healthy/warning/degraded` service state explicitly, `/api/agents/proxy` is no longer a raw bearer mutation tunnel, direct Docker restart/GPU swap routes are operator-gated, the dead dashboard governor queue/dispatch shell remains deleted, dashboard `/api/attention` plus `/api/attention/proactive` now aggregate canonical task-engine data instead of the DEV governor service, the agent-output panel now reads canonical workforce/activity data instead of ghost commit/session telemetry, and `tasks.py` no longer carries the legacy `_update_task()` persistence wrapper. The newest operator-residue tranche is now live end to end: `/v1/operator/summary` carries canonical task residue straight from the task engine, `/v1/tasks/stats` cleanly separates actionable failures, repaired historical failures, stale leases, and approval-held tasks, the stream route now propagates those same residue fields instead of flattening back to run totals, and the digest plus system-pulse surfaces now render canonical approval-held, stale-lease, and actionable-failure posture instead of inferring â€œtasksâ€ from run state. The newest launch-governance tranche now exposes provider weak-lane truth from the provider catalog, latest provider-specific capture per provider, and the VAULT env-name audit, and the latest startup-resilience slice keeps `athanor-agents` alive through dependency or host outages so live `/health` can report the real blocker instead of disappearing. The foundry proving lane is reconciled from live evidence, current bootstrap truth shows durable persistence live with restart proof recorded, and the remaining work is packet-backed runtime follow-through plus ordinary maintenance rather than an active bootstrap blocker. |
| Aggressive prune | In progress | Multiple prune waves removed or archived stale startup, guide, design, hardware, provider, and audit/context docs already replaced by canonical and generated truth; the current wave retired `scripts/validate-atlas.py`, deleted the remaining active atlas completion-audit plan plus the stale atlas topology/UI/API docs and atlas inventory/schema bundle, rewrote the surviving atlas reference docs so they no longer imply the deleted atlas inventory lives under `docs/archive/atlas/`, removed the dead dashboard `getGovernorBaseUrl()` helper, deleted the stale archived daily-operations guide, deleted the redundant short March 18 tactical plan, deleted the retired raw-context and dev-environment archive docs, moved the dated dashboard audits under `docs/archive/audits/`, rewrote `docs/MASTER-PLAN.md` so it no longer carries live-looking provider, hardware, model, or routing truth, archived `docs/BLOCKED.md`, moved the dated Feb 25 hardware deep-audit bundle under `docs/archive/hardware/`, tightened the atlas command-hierarchy reference so it no longer leaves governor durable-task ownership ambiguous, normalized the helper-doc archive path so the historical hardware ledger now points consistently at `docs/archive/hardware/hardware-inventory.md`, archived the stale dashboard spec plus the old automation-backbone provider-plan design doc and three provider-heavy research documents out of active paths, moved the old base-platform, node-role, remote-access, voice-interaction, dashboard, agent-framework, and command-center research pack under `docs/archive/research/` with ADR and design references repointed, deleted the stale `docs/research/2026-02-15-research-roadmap.md` tracker and the active `docs/design/dashboard-interactions.md` control-surface note instead of leaving misleading active-root copies behind, archived the dated `2026-03-14-dev-tool-orchestration.md` and `2026-03-16-local-vs-cloud-meta-orchestrator.md` research notes out of active paths, archived the remaining rack-session bundle plus the stale March 7/9/13/15 orchestration and optimization research notes out of active hardware and research paths, rewrote the surviving `2026-03-09-local-ai-architecture-synthesis.md` reference back to the archived productivity note, rewrote the troubleshooting/reference layer so it points back to provider-catalog and model-deployment truth instead of stale provider-plan prose or raw Vault IP callouts, removed the old `local-system-mind` and Docker-era Open WebUI restart guidance from the active troubleshooting path, rewrote the stale design/atlas authority docs so they no longer attribute durable tasks, leases, or schedules to governor/Athanor, deleted the dead `.claude` plan that still pointed agents at WORKSHOP dashboard and `/queue`, demoted the retained `.claude/scratch` audit artifacts so they read as historical evidence instead of live authority, deleted the stale `scripts/health-check-all.sh` helper, promoted `drift-check.sh` plus `run_service_contract_tests.py` in the scripts inventory, scrubbed stale stuck-queue wording from the operator helper layer, archived `docs/design/capability-codex.md`, `docs/decisions/ADR-012-litellm-routing.md`, `docs/research/2026-02-25-master-synthesis.md`, `docs/research/2026-03-08-data-sovereignty-audit.md`, `docs/research/2026-03-13-coding-models-march-update.md`, and `docs/research/2026-03-14-ai-model-landscape-q1-2026.md` so those stale provider and market-landscape narratives no longer sit in active-root paths, now moves `docs/VISION.md` into `docs/archive/planning-era/VISION.md` so the historical product-intent narrative stops reading like an active-root authority document, and deletes the surviving active-root copies of `docs/architecture/complete-context-raw.md`, `docs/design/athanor-complete-context.md`, `docs/design/automation-backbone-master-plan.md`, `docs/guides/system-map.md`, `docs/guides/system-overview.md`, and `docs/projects/dashboard/SPEC.md` after repointing the remaining atlas and execution-tracker references to archived plan truth. |

## What Is True Now

- `C:\Athanor` is the durable code and control-plane home.
- `main` is the only live local branch, `origin/main` is the only remaining remote ref, and `C:\Athanor` is the only registered worktree; the deleted branch/worktree state is preserved under `C:\Athanor_cleanup_archive\2026-03-30-branch-normalization`.
- `/home/shaun/repos/athanor` on DEV remains the governed runtime and deployment authority root; the 2026-04-08 packet execution brought it back to a mirror-clean state at commit `5148170`, and future resyncs stay packet-backed.
- `/opt/athanor/agents` on FOUNDRY is now an explicit governed deployment root for `athanor-agents`; the live container imports from installed `site-packages`, and the remaining cleanup there is the approved build-root hygiene plus redeploy packet rather than an undocumented hotfix path.
- FOUNDRY `:8100` is the canonical healthy text lane on the deterministic vLLM image lane; the legacy coordinator surface on `:8000` still lists a model but direct chat completions hang, so it is explicitly degraded secondary lineage rather than a healthy primary runtime. WORKSHOP currently exposes the vision runtime on `:8012`, while the old worker lane on `:8010` and Workshop Open WebUI are absent at runtime and now sit in explicit retirement-or-restore truth instead of assumed healthy services.
- VAULT Prometheus plus alert-rules are currently healthy after the governed reconcile pass, and VAULT LiteLLM config is aligned; the remaining VAULT closure work is secret-source repair for blocked provider lanes, not config drift.
- `C:\Users\Shaun\dev\athanor-next` is an incubation lane and cannot silently become primary.
- VAULT `.203` is reachable again from DESK, and direct probes now show Redis `:6379`, Postgres `:5434`, Qdrant `:6333`, and LiteLLM `:4000` answering there.
- `FOUNDRY`, `DEV`, and `WORKSHOP` are reachable again from DESK, and the audited runtime packet set is re-probed closed; the remaining blocker is provider-secret repair rather than host reachability or compose drift.
- Router shadow phase `1` is live, and the atlas currently reports `devstack_primary_build_and_shadow`; governed dispatch is now active off the ranked autonomous queue, the live loop family is `governor_scheduling`, the live burn economy is compounding-ready on local plus subscription lanes, and the provider gate is completed even though optional cloud elasticity repairs still remain.
- The live ranked autonomous queue should be read from `reports/ralph-loop/latest.json` and `python scripts/session_restart_brief.py --refresh`, not from hardcoded counts in this file; current priority still centers on `Validation and Publication`, with dispatch and harvest truth remaining continuity-governed follow-on work.
- Capacity telemetry and quota truth now carry slot-level harvest breakdowns, zone counts, slot-target intent, and scheduler conflict visibility, so local compute is no longer represented only as a collapsed node-level reserve.
- The topology-owned Master Atlas panel now surfaces the live work-economy proof chain directly: work-economy status, burn dispatch phase, degraded quota-family badges, and the local harvest window with open scheduler slots and target pools are visible on the operator front door instead of being buried in JSON.
- Current devstack build-state truth now lives in `C:/athanor-devstack/docs/operations/DEVSTACK-FORGE-BOARD.md`, `C:/athanor-devstack/reports/master-atlas/latest.json`, and the linked packet set instead of in Athanor startup narration.
- Accepted and adopted devstack deltas should be treated as packet-backed lineage and atlas state, not as active Athanor startup queue items.
- Bounded shell and pilot decisions remain explicit in their owned packet, registry, and evidence surfaces rather than in top-entry status prose.
- Mutable control-plane truth belongs in `config/automation-backbone`, not in historical prose.
- The full Dirty13itch GitHub portfolio is now mirrored into machine-readable Athanor truth under `config/automation-backbone/reconciliation-source-registry.json` `github_portfolio`, and `reports/reconciliation/github-portfolio-latest.json` proves the current live set of 35 repos with `Dirty13itch/Wan2GP` as the lone repo still lacking a confirmed local clone.
- The noisiest tenant families are no longer ambiguous: `Field_Inspect-operations-runtime` is the only sibling lane still queued for selective replay into the product root, while the other `C:\Field Inspect-*` lanes are now classified toward lineage, archive, or freeze; the `RFI` `C:\CodexBuild\*` variants are preserved archive evidence only under a dedicated duplicate-evidence packet.
- Three clearly standalone local products are now locked out of Batch 3 ambiguity: `C:\Meds`, `C:\Users\Shaun\Ulrich Energy Auditing Website`, and `C:\Sabrina Ulrich Counseling`.
- `Field_Inspect-operations-runtime` is no longer just a surviving branch name; it now has a canonical replay packet and generated report that lock its file buckets, execution posture, and validation commands.
- The `Field Inspect` safe operations-runtime tranche plus its minimum support set is now landed and validation-green in `C:\Field Inspect`; the remaining open work there is the shared-project/report-delivery second tranche plus sibling-lane freeze follow-through.
- The review-branch-only `RFI & HERS Rater Assistant` stabilization fixes are now landed in the authority root, `python scripts/validate_settlers_ridge_packet.py` is green there, and `scripts/codex/validate-safe.ps1` is the governed validation lane while the spaced root still lacks a usable local Node toolchain.
- Dashboard `test:e2e:audit` now self-prepares the completion-audit inventory through `projects/dashboard/scripts/ensure-completion-audit.mjs`, and the bootstrap runtime/state tests now reuse seeded bootstrap fixtures instead of reseeding a full temp root per case.
- `scripts/vault-ssh.py` now falls back to native OpenSSH when Paramiko auth fails, so repo-side VAULT audits use the same live operator path that already works from DESK instead of producing a false authentication failure.
- The live VAULT LiteLLM env audit now captures real container evidence again: the container is `ghcr.io/berriai/litellm:main-stable`, the launch command is `docker/prod_entrypoint.sh --config /app/config.yaml --port 4000 --num_workers 4`, and the currently present provider env names are `ANTHROPIC_API_KEY`, `CODESTRAL_API_KEY`, `DEEPSEEK_API_KEY`, `GEMINI_API_KEY`, `GOOGLE_API_KEY`, `MISTRAL_API_KEY`, `OPENAI_API_KEY`, `OPENROUTER_API_KEY`, and `VENICE_API_KEY`.
- `config/automation-backbone/operator-surface-registry.json` is the editable truth layer for human-facing surfaces, and `docs/operations/OPERATOR-SURFACE-REPORT.md` is the derived operator report for portal and launchpad posture.
- `runtime.subsystem.task-engine` means durable task ownership plus governor posture, not alternate governor ownership and not a standalone governor facade.
- New registries now exist for hardware, model deployment, provider catalog, subscription-burn, tooling, credential surfaces, repo roots, and routing taxonomy.
- Generated truth reports are the replacement target for stale node, model, provider, and repo-layout narratives.
- `docs/archive/hardware/hardware-inventory.md` is now explicitly the archived historical component ledger; current node truth is the registry-backed hardware report.
- Heuristic provider burn math is being demoted; operator truth should distinguish `official`, `observed`, and `heuristic` states.
- The subscription-burn service now uses provider-catalog pricing truth when available, defaults to `legacy_unverified` when it does not have verified catalog cost, no longer assumes `America/Los_Angeles`, and derives burn lanes plus windows from `config/automation-backbone/subscription-burn-registry.json`.
- `cli-router.py` now derives task-class CLI order from `projects/agents/config/subscription-routing-policy.yaml` plus `config/automation-backbone/provider-catalog.json` instead of a private task-rule and subscription-key table.
- `zai_glm_coding` is now tracked as `configured-unused` instead of `active-routing`, and ordinary execution-lease selection excludes handoff-only lanes unless the request explicitly allows them.
- The live subscription routing policy now carries explicit `routing_posture` and `routing_reason`, and weak provider lanes now carry direct `evidence` contracts in the provider catalog instead of relying on prose or omission.
- Provider-specific LiteLLM proof now has a concrete artifact path at `reports/truth-inventory/provider-usage-evidence.json`, a recording helper at `scripts/record_provider_usage_evidence.py`, and an operator runbook instead of living only as â€œpending captureâ€ prose.
- Routing, subscriptions, and model observatory panels now expose provider summary evidence such as state reasons, recent execution posture, next actions, and recent outcomes instead of limiting operator truth to coarse availability badges.
- The legacy provider-status surface now projects canonical provider posture states such as `available`, `handoff_only`, and `degraded`, and the routing/model dashboard status dots now understand that vocabulary instead of relying on the older `active`/`online` split.
- The active `.claude/skills/delegate/SKILL.md` no longer carries hardcoded provider pricing, quota, or local endpoint tables; it now points back to the provider catalog, routing policy, model deployment registry, and generated reports for live routing truth.
- `docs/TROUBLESHOOTING.md` now sends provider/LiteLLM debugging through the provider catalog, generated provider report, `vault-ssh.py`, and topology-owned localhost probes instead of raw Vault IPs and the stale `provider-status` route.
- `docs/projects/dashboard/SPEC.md`, `docs/design/automation-backbone-master-plan.md`, and the stale provider-pricing research docs now live under `docs/archive/` so they stop presenting historical provider and routing assumptions from active doc paths.
- The agent-side governor snapshot route now reads the explicit canonical builder seam directly, and the dashboard `/api/governor` proxy has direct fixture-backed route coverage instead of relying on transitive governor-surface tests.
- The dashboard governor mutation proxies for pause, resume, presence, and release-tier now have direct wrapper tests that lock their shared operator-envelope forwarding contract to the canonical `/v1/governor/*` paths.
- The agent-side `/v1/governor/tool-permissions` route now returns the canonical governed tool-permissions snapshot instead of a legacy raw agent-metadata matrix, and the dashboard `/api/governor/tool-permissions` proxy now has direct fixture-backed route coverage for that canonical shape.
- The remaining subscription read-model proxies for policy, providers, quotas, and CLI status now have direct fixture-backed route coverage instead of depending on transitive subscriptions-page tests.
- The primary models routes now have direct dashboard route coverage for the inventory snapshot, governance snapshot, and both proving-ground paths instead of relying on transitive model-observatory tests.
- The promotion-controls dashboard routes now have direct coverage for the promotion index plus stage/advance/hold/rollback mutation wrappers instead of relying on transitive governance or observatory tests.
- The digest, improvement, system-map, agents, GPU, and overview dashboard read-model proxies now also have direct fixture-backed route coverage, so those primary operator surfaces no longer depend on page-level or transitive fixture tests alone.
- The alerts, notification-budget, insights, outputs index/file, autonomy, and conversations dashboard proxies now also have direct route-level coverage, so that read-model batch no longer depends on page-level fixtures or implicit server-agent behavior.
- The consolidation-stats, scheduling-status, judge-plane, skill-summary/top, trust, notification-budget, insights, media-overview, GPU-history, Docker container list/logs, Stash stats, STT, TTS fixture/error, the remaining workforce per-item mutation wrappers, the research-job wrappers, the full pipeline route family, push-subscribe state handling, both personal-data routes, the direct `chat` plus `stream` SSE routes, the `operator` / `topology` / `agents/workbench` / `workforce` / `subscriptions` / `digest` / `projects` / `gallery` / `governor` / `workspace` / `routing` / `models` / `tasks` / `workplanner` / `terminal` / `monitoring` / `pipeline` page shells, the topology, services, subscriptions, workforce overview, digest, projects, gallery, governor, workspace, routing, model-observatory, task-board, work-planner, monitoring, terminal, and pipeline console behaviors, and the deeper operator plus agent-workbench console flows now have direct coverage, and the full dashboard matrix is green at `207` test files / `352` tests after the latest provider-execution checkpoint tranche.
- The routing console read-model routes for provider status and routing log now have direct fixture-backed dashboard route coverage instead of relying on classify-only route tests and page-level fetches.
- Provider execution now derives direct CLI, bridge CLI, handoff, command-name, and adapter-default truth from the provider catalog instead of a duplicated hardcoded execution table, so bridge advertisement alone cannot silently override catalog execution-mode policy.
- The subscription execution, leases, handoffs, and policy dashboard wrappers now have direct proxy-contract coverage, and the fixture `/v1/subscriptions/policy` payload now matches the live backend shape by returning a provider mapping instead of a string array.
- Provider-execution handoff payloads now serialize timestamps consistently at the API boundary while preserving numeric timestamps for internal lease and posture logic.
- The primary projects routes now have direct dashboard route coverage for the project index, stalled-project snapshot, milestone read/write path, and project advance mutation instead of relying on page-level project console coverage alone.
- The MCP Athanor agents bridge now reads governor posture from `/v1/governor` and queue truth from canonical `/v1/tasks/stats` instead of treating the governor payload as task truth.
- Live `/v1/governor/operations`, `/v1/governor/operator-tests`, and `/v1/governor/operator-tests/run` now use the canonical readiness and synthetic-operator-test builders instead of returning thin compatibility payloads, and the dashboard now has explicit proxy and smoke coverage for those snapshots.
- `docs/MASTER-PLAN.md` is now strategy-only reference material and no longer carries live provider pricing, reset, hardware, deployment, or routing truth.
- The dated dashboard audits no longer live in the active docs root; they now sit under `docs/archive/audits/` with explicit archive banners.
- The stale archived daily-operations guide, retired dev-environment notes, raw context export, redundant short March 18 tactical plan, the active atlas completion-audit plan, atlas API/topology/UI docs, and the stale atlas inventory/schema bundle are gone instead of lingering as misleading archive debt.
- Secret-bearing runtime surfaces must be reported by presence and location only, never by value.
- Credential delivery surfaces now have explicit target delivery methods, remediation states, and approval boundaries in the control plane.
- Redis-using script lanes now support an optional managed local env file at `~/.athanor/runtime.env` or `ATHANOR_RUNTIME_ENV_FILE`; the credential still must exist outside tracked source.
- A dedicated repo-side runbook now exists for the ask-first DEV secret-delivery cleanup pass, and the live DEV cron/systemd estate now matches that envfile-backed contract.
- A dedicated repo-side runbook now preserves the completed `athanor-governor` retirement sequence on DEV, active-doc references to `:8760` or `athanor-governor` are now quarantined to explicit troubleshooting or retirement-runbook lanes instead of drifting back into general operator guidance, and the runbook now encodes the verified backup root, rollback-only packet flow, no-new-`/queue`-or-`/health`-traffic acceptance checks, and per-caller rollback handling for the retired seam.
- The generated [RUNTIME-MIGRATION-REPORT.md](/C:/Athanor/docs/operations/RUNTIME-MIGRATION-REPORT.md) now serves as the verified audit record for the completed 2026-03-29 DEV cutover, including sync order, runtime targets, rollback targets, systemd backup target, and post-cutover live content-match evidence for all 9 mapped callers.
- The DEV `:8760` cutover is complete; autonomy expansion is no longer blocked on the retired governor facade, but it remains policy-scoped and approval-gated.
- `https://athanor.local/` is the canonical human front door, DEV `:3001` is the transitional runtime target behind Caddy, and WORKSHOP `:3001` is retired and no longer reachable as a command-center portal.
- `config/automation-backbone/autonomy-activation-registry.json` now owns the post-cutover activation scope, and [AUTONOMY-ACTIVATION-REPORT.md](/C:/Athanor/docs/operations/AUTONOMY-ACTIVATION-REPORT.md) is the generated operator-facing view of that scope.
- The first post-cutover activation wave is software-core only: coding, research, knowledge, general-assistant, and governed backlog or self-maintenance loops.
- The follow-up autonomy-enforcement tranche wired that scope into the governor runtime and retry path, so `scheduler`, `pipeline`, and `auto-retry` no longer auto-execute merely because they are operational sources; they now stay held until the current autonomy phase is explicitly enabled, blocked workloads such as the DPO training background transform remain approval-gated even after enablement, and autonomy-managed plans plus decomposed tasks now re-filter assigned agents against the current phase before they hit task submission.
- The autonomy registry now promotes `full_system_phase_3` to the active live phase, `broad_autonomy_enabled` is true, and runtime ownership is governed by `config/automation-backbone/runtime-ownership-contract.json`, `config/automation-backbone/runtime-ownership-packets.json`, `docs/operations/RUNTIME-OWNERSHIP-REPORT.md`, and `docs/operations/RUNTIME-OWNERSHIP-PACKETS.md` instead of undocumented operator memory.
- Runtime mutations remain approval-gated even after first activation.
- The truth collector can now probe DEV runtime ownership and DESK local runtime-env presence without printing secret values, and the DESK managed runtime env currently resolves Redis auth successfully.
- The 2026-03-29 truth collector now proves `athanor-governor.service` is absent on DEV, no listener remains on `:8760`, observed runtime references have dropped to zero, the 9 mapped runtime-owned callers now resolve to synced implementation-authority copies, and the live caller map still matches the runtime-migration registry with zero unmapped callers.
- The retired standalone governor facade is now deleted from implementation authority; the migration seam remains documented only in reports, rollback evidence, and the retirement runbook.
- The latest prune wave moved the stale early ADR-support research bundle under `docs/archive/research/`, including the original base-platform, network, storage, node-role, inference-engine, creative-pipeline, dashboard, agent-framework, monitoring, home-automation, media-stack, and research-roadmap documents, and repointed ADR-001 through ADR-011 plus the remaining live cross-references at those archived locations instead of leaving early planning-era guidance in active doc paths.
- The provider report and live provider snapshot now distinguish Vault proxy activity from provider-specific upstream usage, so configured API lanes no longer present LiteLLM host activity as proof that a specific remote provider has been exercised recently.
- `scripts/probe_provider_usage_evidence.py` now records provider-specific Vault LiteLLM captures against real served model ids instead of stale alias assumptions.
- The current live provider-specific probe proves `anthropic_api`, `deepseek_api`, `mistral_codestral_api`, `moonshot_api`, `venice_api`, and now `zai_api` via successful Vault LiteLLM completions; `openai_api`, `google_gemini_api`, `dashscope_qwen_api`, and `openrouter_api` remain explicitly classified as optional provider-specific auth or auth-mode failures.
- The implementation-authority `ansible/roles/vault-litellm` role now exports the full template-owned provider env contract instead of only Anthropic, and repo contracts now fail if the LiteLLM template starts requiring an `os.environ/*` key that the tracked container env block does not export.
- The VAULT LiteLLM credential surface remains optional-elasticity maintenance because the latest live container env-name audit shows `ANTHROPIC_API_KEY`, `CODESTRAL_API_KEY`, `DEEPSEEK_API_KEY`, `GEMINI_API_KEY`, `GOOGLE_API_KEY`, `MISTRAL_API_KEY`, `OPENAI_API_KEY`, `OPENROUTER_API_KEY`, `REDIS_PASSWORD`, `VENICE_API_KEY`, and `ZAI_API_KEY` present while only `DASHSCOPE_API_KEY` and `MOONSHOT_API_KEY` remain missing.
- The VAULT LiteLLM env-name audit is now a first-class truth artifact at `reports/truth-inventory/vault-litellm-env-audit.json`; `docs/operations/SECRET-SURFACE-REPORT.md` and `docs/operations/PROVIDER-CATALOG-REPORT.md` consume it directly so each blocked lane now reports exact missing env names plus current owner-surface evidence instead of generic repair prose.
- The latest owner-surface pass now makes the current VAULT LiteLLM story explicit: `docker.config.json` has no `litellm` template mapping, `container-watchdog` monitors the container, and the historical `litellm.inspect.*.json` backups show the live container itself has been the practical secret-delivery surface across multiple env-set revisions.
- `docs/operations/VAULT-LITELLM-AUTH-REPAIR-PACKET.md` now turns the cached env audit and provider-usage evidence into a deterministic VAULT maintenance checklist with exact missing env names, backup commands, and re-probe steps.
- The VAULT env audit now also records the live `litellm` launch command and the only current `/boot/config` reference file, which narrows the visible runtime-owner surface further while still classifying the deployment as a standalone container rather than a dockerMan or compose-managed one.
- The 2026-03-29 read-only VAULT scan did not identify a dockerMan template or host envfile that obviously owns the `litellm` container by name, so the current repair packet treats the live container definition itself as the visible runtime authority until a managed source is captured.
- The same 2026-03-29 read-only VAULT scan showed the missing provider keys are absent from the host shell env as well, so the remaining blocked Vault API lanes are now mostly blocked on introducing or locating real secret material plus upstream auth-mode or credential-validity fixes for the still failing present-key lanes.
- Redis-backed automation evidence persists again from DESK through the managed local runtime env surface.
- Recovery evidence now redacts credential-bearing URLs before writing artifacts to disk.
- The truth collector now probes `openfang`, `langfuse`, `miniflux`, and `neo4j_http` through topology-owned health paths and treats `neo4j` plus `redis` as TCP-connect planes, so collector output no longer reports those services through fake unsupported-HTTP errors.
- The DESK provider bridge now emits the shared health snapshot shape and requires a forwarded operator-action envelope before it shells out to local provider CLIs.
- Privileged task create/approve/cancel/reject/review, research job create/execute/delete, project milestone/advance/supervise, workforce goal create/delete, notification resolution, preference writes, explicit and implicit feedback, gallery feedback, event ingestion, pipeline steering, workspace mutation, workplan generation/redirect, and convention propose/confirm/reject routes now require the shared operator-action envelope and emit accepted or denied audit events instead of carrying legacy direct-caller bypasses.
- Skill create, execution recording, and delete routes now also require the shared operator-action envelope and emit accepted or denied audit events.
- Scheduled-job listing is back in the live task router, and manual scheduled-job runs now require the shared operator-action envelope and emit accepted or denied audit events.
- Emergency stop and resume now require the shared operator-action envelope directly on the agent server, with destructive-admin posture on the kill switch path and accepted or denied audit events for both endpoints.
- Core-memory update, digest generation, workflow execution, and the agent-side Workshop GPU swap route now also require the shared operator-action envelope and emit accepted or denied audit events.
- Quality-gate now fails closed correctly on denied operator-action envelopes, has explicit service-contract tests for health/auth/destructive cleanup gating, declares its Redis-backed operator-audit dependency in `services/quality-gate/requirements.txt`, and is registered as a platform-core acceptance gate instead of an untracked core service.
- `python scripts/run_service_contract_tests.py` now owns the repo-local service-contract bundle for quality-gate, gateway, governor, brain, classifier, and sentinel, so service verification no longer depends on whatever base interpreter happened to have `pytest` installed.
- The service-contract bundle now also owns direct pytest suites for `subscription-burn` and `semantic-router`, so the script-resident control-plane surfaces no longer rely on shell liveness checks alone.
- `scripts/operator_contract.py` now resolves Redis audit transport from runtime env or topology-owned service truth instead of a hardcoded Vault address, and `scripts/service_contract.py` now degrades health only on required dependencies.
- External task-state callers now use `tasks.persist_task_state()` instead of importing the private `_update_task()` helper, and repo contracts enforce that boundary.
- The dashboard proxies for research-job creation/execution and project milestone creation/advance now forward the shared operator envelope instead of raw JSON bodies.
- The dashboard proxies for workforce goals, notification resolution, and pipeline steering actions now also forward the shared operator envelope instead of raw JSON bodies.
- The dashboard proxies for workplan generation/redirect and workspace endorsement now also forward the shared operator envelope instead of raw JSON bodies.
- The dashboard proxies for convention confirm/reject now also forward the shared operator envelope instead of raw JSON bodies.
- The dashboard `skills` write routes now also forward the shared operator envelope instead of raw JSON bodies, and dashboard auth classifies `skills` mutation paths as privileged.
- The dashboard scheduled-job manual-run proxy now forwards the shared operator envelope instead of raw JSON, and the dashboard route is no longer pointed at a ghost endpoint that only existed in `server.py.bak-codex`.
- The dashboard governor-heartbeat proxy now also forwards the shared operator envelope instead of a legacy raw JSON pass-through.
- Terminal bridge ticket issuance now signs the operator action envelope into the bridge ticket, and `ws-pty-bridge` emits best-effort accepted or denied audit events for upgrade/auth, disallowed-node, capacity, and PTY spawn outcomes.
- The dashboard preference and explicit/implicit feedback proxies now also forward the shared operator envelope instead of raw JSON bodies, and dashboard auth classifies those mutation paths as privileged.
- The dashboard services and overview summary layer now counts `warning` services separately from `healthy` and `degraded`, so shell, topology, and services views stop collapsing shared-health warning state into the legacy boolean `healthy` count.
- The dashboard service aggregator now treats reachable-but-invalid health payloads as `warning` contract drift instead of silently calling them healthy, so bespoke or malformed core health endpoints surface as repair work rather than disappearing into the green count.
- `services/gateway` now has direct shared-health contract tests, CI coverage, and a platform-core registry entry instead of relying on transitive dashboard coverage.
- The dashboard fixture `system-map` payload now matches the real authority split: governor owns posture and fallback policy, while durable tasks, leases, and schedules belong to the task engine, subscription broker, and scheduler.
- The legacy governor `/health` endpoint now emits the shared health snapshot shape, intentionally reports `degraded`, and no longer exports fake subscription or spend truth; dead inventory/session routes now fail closed with explicit `410` retirements, and the standalone `:8760` service identity remains outside canonical topology with the live DEV listener now retired.
- The gateway `/health` endpoint now emits the shared health snapshot shape with `read-only` auth semantics while preserving its paged dependency list for compatibility, so the dashboard can parse gateway health without a bespoke fallback.
- `brain`, `classifier`, and `sentinel` now emit the shared health snapshot shape instead of bespoke `ok` payloads; `brain` reports refresh and scheduler dependency state, `classifier` reports guard-model load state, and `sentinel` reports per-tier monitoring health.
- Dashboard-local `/api/operator/nav-attention` and `/api/operator/ui-preferences` writes now enforce the same privileged operator-session gate already declared in the route contract instead of persisting unauthenticated JSON writes.
- Dashboard `/api/context/preview` and `/api/routing/classify` now proxy through the shared operator-action helper, and the agent-side diagnostic POST handlers require the shared operator envelope plus accepted/denied audit events instead of accepting raw unaudited POST bodies.
- The generic dashboard `/api/agents/proxy` helper now requires an operator session for reads, the standard privileged-mutation gate for writes, and only allows `/health` or `/v1/*` agent paths.
- The direct dashboard Docker mutation routes for container restart and Workshop GPU mode swap now also require the dashboard privileged-mutation gate instead of relying on raw local access.
- Dashboard `/api/attention` and `/api/attention/proactive` now aggregate canonical task-engine, proposal, and service-health data instead of hitting the DEV governor service on `:8760`.
- The dashboard agent-output panel now reads canonical `/api/workforce` and `/api/activity` data, and the dead `/api/governor/commits` plus `/api/governor/sessions` routes are gone.
- `tasks.py` now uses `persist_task_state()` directly everywhere and no longer retains the legacy `_update_task()` wrapper.
- `tasks.py` now routes auto-retry submit, initial submit, cancel, and stale-lease recovery through `persist_task_state()` as well, so the canonical persistence seam is no longer bypassed by internal lifecycle helpers.
- Manual task submission, proactive scheduler submissions, work-planner task generation, workspace reactions, and pipeline decomposition now all flow through the canonical `submit_governed_task()` helper, so governor gate reasoning, metadata stamping, and approval-hold persistence no longer drift across five separate producers.
- The legacy governor helper scripts `services/governor/self_improve.py` and `services/governor/overnight.py` now feed the canonical `/v1/tasks` and `/v1/tasks/dispatch` endpoints instead of the retired alternate governor queue and `/dispatch-and-run` path.
- The remaining legacy governor reporting helpers `services/governor/act_first.py` and `services/governor/status_report.py` now read canonical `/v1/tasks` and `/v1/tasks/stats` instead of the retired queue/status surfaces, and `act_first.py` no longer pretends to mutate fetched queue records.
- Sentinel integration checks now probe canonical `/v1/tasks/stats` on the agent server instead of the legacy governor `/queue` surface.
- `services/governor/main.py` and its dedicated compatibility tests are deleted from implementation authority now that the runtime cutover is verified, so no standalone governor service code remains in the active repo.
- The retired governor-local SQLite and dispatch implementation is no longer active repo truth: `services/governor/dispatch.py`, `services/governor/continuous_dispatch.py`, `services/governor/db.py`, and the old archived `governor.db` snapshot are deleted, drift-check now verifies compatibility mode instead of SQLite WAL state, and troubleshooting docs no longer tell operators to inspect the old DB as live task truth.
- The standalone governor-facade end-state decision is closed: runtime authority no longer needs it, and repo code may not recreate it as a second task-engine surface.
- `task_store.py` no longer carries duplicate status-reader definitions, `tasks.py` no longer carries duplicate event-publish helpers, and the task engine now uses indexed status reads instead of whole-hash scans for common cleanup, deduplication, listing, and stats paths.
- Digest generation now reads only indexed terminal task statuses instead of scanning the full Redis task hash, and repo contracts now forbid `read_all_task_records()` from leaking above `task_store.py`.
- `/v1/metrics/agents` now uses canonical `get_task_stats()` instead of swallowing a dead `get_stats` import, so per-agent task totals flow from the durable task engine instead of disappearing behind the route's broad exception guard.
- `list_tasks()` now supports canonical multi-status reads and keeps agent filtering above the store-read limit, so higher-level consumers stop dropping matching tasks behind pre-filter truncation.
- `owner_model._gather_capacity()` now uses one uncapped canonical pending+running task read instead of two `limit=50` snapshots, so busy agents stop being misclassified as idle when the queue grows.
- The terminal Playwright harness now uses the bridge `spawnPty` test seam plus `bridge.close()` cleanup instead of a real Windows ConPTY PowerShell child, so the operator-flow browser test stays real without the post-pass `AttachConsole failed` stderr noise.
- `tasks.py` no longer drops task-submitted or task-retried events on an undefined Redis handle, and its hottest pending-task and status-filtered reads now use the durable task-status secondary indexes instead of rescanning the full Redis task hash.
- `list_recent_tasks()` now reads the canonical Redis updated-index instead of the queue-oriented pending-first list view, so execution-run history and work-planner recent context no longer get crowded out by large pending queues.
- Judge summary queue counts and notification unread counts now use canonical `pending_approval` task stats instead of capped preview snapshots, and the work planner now reads pending plus pending-approval tasks directly instead of inferring backlog posture from a mixed recent-task slice.
- No active implementation-authority service file may keep the retired governor `/queue` or `/dispatch-and-run` route inventory; those tokens now fail the repo contract outright.
- Workforce notifications now surface `approval_preview_count`, `approval_preview_limit`, `approval_total_count`, and `approval_preview_truncated`, so the operator can see when the visible approval list is only a preview of the canonical backlog.
- The completion-audit runtime subsystem census now uses the live `athanor_agents.server.app` route registry plus dashboard navigation/runtime-subsystem registry metadata, classifies live agent-server endpoints into runtime subsystems, and writes those inventories under `reports/completion-audit/latest/inventory/`.
- The completion-audit runner now uses the project-local agents pytest suite instead of false-negative `unittest discover`, so failed audit jobs reflect real suite failures rather than missing pytest imports.
- `/api/langfuse/costs` now returns an honest degraded `200` payload instead of surfacing same-origin `502` errors, and the monitoring console renders that degraded state explicitly instead of relying on transport failure.
- The completion-audit runner now reaches `ready` state again from `python scripts/run-completion-audit.py --skip-live`; blockers and warnings are closed for the current census, and the remaining work is route-audit depth rather than audit readiness.
- `/pipeline` and `/workforce` now register as `live_complete` in the completion-audit route census instead of hanging in authored-coverage drift.
- Dashboard Playwright mutation coverage is green again, and valid header-authenticated mutation requests now backfill a generated operator envelope `session_id` when no session-id cookie is present.
- The stale atlas topology/UI/API layer and atlas inventory/schema bundle are archived under `docs/archive/atlas/`, and `scripts/validate-atlas.py` is retired; the live atlas is now a narrow reference lane over current registries, reports, and retained governance/runtime synthesis docs.
- `docs/MASTER-PLAN.md` now stays at the strategic layer instead of carrying live provider, hardware, model, or routing details, and the redundant archive-era guide/plan/context docs that were still leaking stale provider and runtime assumptions have been deleted.
- The agent-server governor router no longer re-registers `POST /v1/tasks/batch-approve` or `POST /v1/tasks/{task_id}/reject`, so the live FastAPI app no longer carries duplicate canonical task mutation paths.
- `docs/BUILD-MANIFEST.md` is now archived at `docs/archive/BUILD-MANIFEST.md`; active planning, intent mining, and operator `/build` guidance now point at `STATUS.md` and `docs/operations/CONTINUOUS-COMPLETION-BACKLOG.md`.
- Diagnosis auto-remediation now fails closed behind the shared `destructive-admin` operator envelope and emits accepted or denied audit events instead of exposing raw shell-trigger routes.
- The first prune wave removed `docs/ARCHITECTURE-OVERVIEW.md` and `docs/DEV-STACK-GUIDE.md`, and the second wave deleted `docs/AGENTS.md`, `docs/guides/system-map.md`, `docs/guides/system-overview.md`, and `docs/design/athanor-complete-context.md` while archiving the remaining stale audit, environment, hardware-map, and raw-context artifacts.
- The current prune wave removed `docs/hardware/loose-inventory.md` and `docs/hardware/CLAUDE.md`, archived the old build manifest, moved the historical hardware ledger to `docs/archive/hardware/hardware-inventory.md`, moved the stale Feb 14 DEV/network/node audit bundle to `docs/archive/hardware/`, repointed the helper layer to the generated hardware report plus the live backlog, and scrubbed the remaining non-archive design/research docs that still pointed at `docs/BUILD-MANIFEST.md` or `docs/hardware/inventory.md`.
- The newest prune slice also moved the dated rack-session plan, node1 quick-reference card, and March 14 hardware audit under `docs/archive/hardware/`, and moved `ATHANOR-SYSTEM-AUDIT.md` under `docs/archive/audits/` so those stale hardware and audit artifacts stop appearing in active-looking locations.
- The newest prune slice also archived `docs/BUILD-ROADMAP.md` and deleted the stale archived dependency graph, so neither the active layer nor the archive root still carries the old deployment-phase roadmap or the obsolete Gateway/MIND/Memory plus standalone-governor dependency map as living reference truth.
- The latest prune slice also moved the old Phase 11 and Phase 16 audit reports out of the active `audit/` root into `docs/archive/audits/`, corrected the atlas command hierarchy so governor authority no longer claims durable tasks, leases, or schedules, updated the architect agent instructions so they no longer describe governor task truth as SQLite-backed, rewrote the stale design/atlas authority docs so they point durable tasks, schedules, and leases back to the task engine, scheduler, and subscription broker, and deleted the dead `.claude` plan that still pointed at WORKSHOP dashboard and `/queue`.

## Reconciliation Exit Gates

| Gate | State | Notes |
|------|-------|-------|
| Authority Gate | Completed | `C:\Athanor` is the only implementation authority and side roots no longer act as hidden current truth. |
| Current-State Truth Gate | Completed | `STATUS.md`, backlog, operating-system truth, completion registry, and Ralph-loop state now converge materially. |
| Runtime Gate | Completed | Major live runtime drift is reconciled and re-probed; runtime ownership is now explicit, packet-backed maintenance instead of an active reconciliation blocker. |
| Provider Gate | Completed | Provider truth is explicit, the turnover-critical Z.ai overflow lane is restored, and the remaining weak API lanes are intentionally demoted optional-elasticity repairs rather than a handoff blocker. |
| Portfolio Gate | Completed | Repo/root classification debt is closed and no meaningful source remains in maybe-important limbo. |
| Product Gate | Completed | `Field Inspect` and `RFI & HERS` no longer act as unresolved active reconciliation ambiguity. |
| Validation Gate | Completed | Validator, docs index, and truth-report gates are the routine acceptance surface. |
| Steady-State Gate | Steady-state monitoring | The current clean-cycle count now exceeds the two-cycle threshold; remaining runtime/provider work is explicit approval-gated or optional-elasticity maintenance rather than active ambiguity cleanup. |

## Immediate Order

1. Treat `capacity-and-harvest-truth` as the live top self-improvement workstream: slot-level scheduler truth is now explicit, so the next threshold is using that contract to steer actual local dispatch and harvest admission instead of leaving compounding economics implicit.
2. Keep `dispatch-and-work-economy-closure` immediately behind it; the next threshold is no longer control-plane convergence, it is keeping Ralph’s ranked queue fed with real Athanor-native compounding work instead of checkpoint hygiene alone.
3. Keep `validation-and-publication` green while the new active workstreams land; all six publication slices are still `ready_for_checkpoint`, so generated truth and validator freshness remain the acceptance floor for every tranche.
4. Treat devstack build-state ordering as owned by the forge board, atlas, and packet set rather than by Athanor startup prose.
5. Keep adopted and accepted build deltas in packet-backed lineage until a new bounded change is explicitly opened.
6. Keep shell-path changes, bounded tooling adjustments, and pilot decisions packet-backed and evidence-backed instead of narrating them as top-entry startup work.
7. Operate `full_system_phase_3` under the explicit provider, sovereignty, and runtime-mutation boundaries in `config/automation-backbone/autonomy-activation-registry.json` and `projects/agents/config/subscription-routing-policy.yaml`, so broader scope does not silently widen onto unverified cloud or VAULT lanes.
8. Keep the VAULT LiteLLM auth-repair packet current as optional-elasticity maintenance: Z.ai is restored, DashScope still needs a real key, OpenAI/OpenRouter/Gemini still need present-key or auth-surface repair, and direct `MOONSHOT_API_KEY` closure remains optional while the observed `kimi-k2.5` alias path stays canonical.
9. Verify the subscribed Kimi billing tier, keep the GLM supported-tool proof fresh so the lane can stay explicitly demoted until promotion is intended, and keep the launch-governance weak-lane queue aligned with the provider catalog, latest per-provider capture truth, and the VAULT env-name audit.
10. Keep the collector-driven live bootstrap artifact sync authoritative so `reports/bootstrap/*.json`, generated reports, and the live FOUNDRY control plane stay aligned instead of drifting back to DESK-local supervisor state once FOUNDRY is reachable again.
11. Keep the restored SSH-backed VAULT operator path, runtime-ownership contract, and generated sync checklists current so runtime-owned caller surfaces (`services/cluster_config.py`, `services/gateway/main.py`, and `services/sentinel/checks.py`) stay explicit authority evidence instead of quiet host drift.
12. Continue pruning stale reference docs that still preserve retired standalone governor, pre-registry service, or old rack-layout narratives, and prepare the merged worktree and branch cleanup packet for explicit approval instead of deleting blind.
13. Keep dashboard work in sidecar mode for lower-risk breadth only after provider truth, autonomous dispatch truth, and canonical residue surfaces stay coherent on clean `main`.
14. Keep full-system autonomy live through the registry-backed phase contract while future DEV resyncs stay packet-backed governed maintenance instead of drifting back into undocumented runtime ownership.

## Required Verification

- `python scripts/validate_platform_contract.py`
- `python scripts/generate_documentation_index.py --check`
- `python scripts/generate_project_maturity_report.py --check`
- `python scripts/generate_truth_inventory_reports.py --check`
- `python scripts/collect_truth_inventory.py`
- `python scripts/run_service_contract_tests.py`
- `cd projects/dashboard && npm test`
- `cd projects/dashboard && npm run typecheck`
- `cd projects/dashboard && npm run build`
- `cd projects/dashboard && npm run test:e2e:terminal`
- `cd projects/dashboard && npm run test:e2e:audit`
- `cd projects/agents && C:\Athanor\projects\agents\.venv\Scripts\python.exe -m pytest tests -q`
- `cd projects/gpu-orchestrator && C:\Athanor\projects\gpu-orchestrator\.venv\Scripts\python.exe -m pytest tests -q`
- `cd projects/ws-pty-bridge && npm run ci`

## Current Open Risks

- The live cluster is reachable again from DESK, and the audited runtime packet set is re-probed closed. The remaining VAULT LiteLLM work is optional-elasticity maintenance for DashScope/OpenAI/OpenRouter/Gemini rather than a turnover-critical convergence blocker.
- Runtime ownership is explicit enough for steady-state maintenance, and the DEV repo-root sync path is now proven by execution. The remaining runtime debt is no longer broad drift; it is concentrated in the VAULT LiteLLM provider-secret lane and in keeping future resyncs packet-backed instead of ad hoc.
- The blocked or demoted VAULT LiteLLM API lanes remain explicit maintenance debt that must stay demoted or be repaired intentionally instead of drifting back into ordinary routing.
- The platform is ready for low-touch devstack turnover at the atlas layer, but remaining packet- or approval-gated build deltas should now be read from devstack board, atlas, and packet truth rather than from Athanor startup prose.
- The repo now syncs live bootstrap artifacts out of the running FOUNDRY container during truth collection, but that path is still only collector-driven; running the DESK-local bootstrap supervisor directly can still recreate stale local snapshots until the deploy/runtime authority contract is tightened further.
- Some runtime ownership facts still live outside the registries in DEV systemd, cron, `/opt/athanor`, and `/home/shaun/.athanor`, even though the collector can now audit them and the reviewed secret-delivery surfaces are normalized.
- Local branch and worktree sprawl can still obscure real drift: `main` now carries the synthesized Athanor-core line, but many obsolete worktrees remain pinned to subsumed branches until the approval-gated cleanup tranche is executed.
- The operator front door is now operationally coherent on DESK through the scripted hosts-file rollout. Additional operator desktops would still need the same helper or internal DNS before they can rely on `athanor.local` without local aliasing.
- A few reference docs still describe obsolete hardware, provider, or repo topology and should be deleted after report generation; the biggest stale archive-era guide, context, atlas, planning, and active-root audit duplicates are gone, but other stale reference narratives still need the same treatment.
- Shared health/action rollout is still incomplete beyond the now-aligned task, feedback, preference, event-ingestion, and related mutation routes.
- Governor/task authority debt is smaller now, and the canonical governed-submission seam is closed. The standalone governor facade no longer runs on DEV and no longer exists in implementation authority, so the next work is downstream provider evidence, prune follow-through, and the remaining shared-health seams.
- Provider catalog truth is stable for the active scheduler lanes, the report now distinguishes routing-enabled CLI lanes lacking observed tools from Vault-observed LiteLLM API lanes, flags live-burn subscription lanes with cost-unverified monthly tiers, exposes inline pricing-truth status, and now emits explicit next-verification actions, but configured API and unverified-cost lanes still need deeper observed-usage evidence.
- Bounded pilot and policy-sidecar work should not drift back into the active startup story unless a new explicit packet or review decision reopens them.
- `runtime.subsystem.task-engine` now explicitly means durable task truth plus governor posture only, not alternate governor ownership and not a retained standalone facade implementation.

## Next Actions

1. **Keep the remaining optional-elasticity VAULT maintenance lane narrow** — Z.ai is restored and the turnover-critical provider gate is closed. DashScope still needs a real key, OpenAI/OpenRouter/Gemini still need auth-surface repair or key rotation, and any live maintenance pass should be followed immediately by a provider re-probe. The DEV runtime repo sync, VAULT Prometheus plus alert-rules lane, FOUNDRY agents/GPU Orchestrator/vLLM lanes, and the Workshop control-surface/product/vLLM lanes are already re-probed closed.
2. **Re-probe provider truth after VAULT repair** — Promote only repaired lanes with both config and observed proof; keep unrepaired VAULT API lanes and weak-proof Kimi/GLM lanes explicitly demoted where evidence still does not support promotion.
3. **Keep build-state truth in devstack** — Use devstack board, atlas, scorecard, and packets for current build ordering instead of reproducing that state in Athanor startup prose.
4. **Keep shell and pilot changes bounded and packet-backed** — Resolve future shell-path, tooling, or pilot changes through their owned packets, registries, and evidence surfaces before widening scope.
5. **Finish freeze/prune follow-through** — Remove remaining stale current-root operational narratives and keep the Ralph loop as the permanent steady-state execution model.
6. **Hold steady-state monitoring under explicit gates** — Keep the end-state gates current and reopen remediation immediately if authority, provider, portfolio, or top-entry truth drift returns.
7. **Checkpoint publication cleanly** — All six bounded publication slices are now `ready_for_checkpoint`; publish the control-plane and closure tranche in those explicit slices instead of letting the working tree become the only source of progress.


