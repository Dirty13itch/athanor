# Truth Convergence Backlog

This is the live execution backlog for the Athanor truth-convergence program. It tracks the active authority model, inventory/report work, drift closure, and prune order.

## Lane Status

| Lane | Scope | Exit Criteria | Current Blockers | Last Verification |
|------|-------|---------------|------------------|-------------------|
| Authority model | Startup docs, repo roots, implementation/runtime split | Startup docs state the correct authority model and validator enforces it | Startup docs were stale and contradictory | Pending truth-layer validation pass |
| Truth inventories | Hardware, models, providers, subscription burn, tooling, credentials, repo roots, routing taxonomy | All eight registries exist, validate, and map active runtime surfaces | Runtime ownership evidence still needs to be pulled further back from DEV into registries and reports | Registries, generated reports, and the truth collector now land cleanly, include repo-safe DEV/DESK runtime evidence, honor topology-owned non-default service health paths, classify `bolt://` plus `redis://` planes by TCP-connect truth instead of fake unsupported-HTTP failures, include a generated runtime-migration report for the retired `:8760` seam, preserve `GOVERNOR-FACADE-CUTOVER-PACKET.md` as completed-cutover audit and rollback evidence instead of pending maintenance tooling, cross-check the observed DEV caller set against that migration map with zero unmapped callers, persist the latest collector snapshot under `reports/truth-inventory/latest.json`, hash each observed runtime-owned caller against implementation authority so the migration report can surface live content-match state directly, and now include `subscription-burn-registry.json` as the canonical burn-lane, schedule, and quota-semantics authority |
| Dashboard subscription truth | Provider catalog adoption and removal of fake burn truth | Dashboard subscription UI and fixture mode use provider catalog-backed fields and label estimates honestly | Kimi subscribed-tier verification and multiple Vault upstream credentials still need runtime-visible proof or repair | Dashboard and fixture surfaces now use catalog-backed provider ids and honest pricing states; operator panels now surface provider summary evidence fields such as state reasons, recent execution posture, recent outcomes, and next actions; the provider report now distinguishes configured CLI lanes without observed tools from Vault proxy-active API lanes that still lack provider-specific request evidence, flags cost-unverified live-burn subscription lanes, shows inline pricing-truth status per provider, emits explicit next-verification actions for weak lanes, now renders a ranked verification queue that pulls Kimi cost verification, GLM CLI restoration, and provider-specific API evidence to the top, and now emits provider-specific verification steps instead of generic follow-up prose; the newest routing tranche demoted `zai_glm_coding` out of live task-class selection until CLI evidence returns, and the lease selector now filters handoff-only providers out of ordinary auto-routing while still allowing explicit governed-handoff consideration; the latest provider-confidence tranche also makes `routing_posture` plus `routing_reason` explicit in policy, carries those fields through execution and compatibility payloads, stores direct evidence contracts in the provider catalog for Kimi, GLM, and the Vault LiteLLM API lanes, and adds the `reports/truth-inventory/provider-usage-evidence.json` artifact plus `scripts/record_provider_usage_evidence.py` so provider-specific LiteLLM proof has a reviewable writeback path; the newest official-doc pass corrected the weak flat-rate semantics so Kimi now records membership-included quota behavior, Z.ai now records published GLM Coding Plan public floor prices plus quota-window semantics, Gemini API now points at official pricing plus billing docs, Moonshot API now cites a pricing-specific official update instead of the generic platform root, and DashScope now points at Alibaba Model Studio pricing instead of the generic model list page. The old provider-status compatibility surface now projects canonical provider posture instead of maintaining a separate thinner availability model; the remaining stale provider-truth panel mocks now use canonical provider posture values; the active `.claude` delegate skill now routes back to the provider catalog and model-deployment registry instead of carrying a stale provider/model table; the live provider snapshot logic now mirrors that stricter evidence posture instead of overstating configured Vault API lanes as observed provider usage; legacy non-catalog scheduler lanes were deleted from the live burn surface on 2026-03-26, `scripts/subscription-burn.py` now derives live scheduler lanes, schedule windows, and quota semantics from `subscription-burn-registry.json` while leaving pricing truth in the provider catalog, `scripts/cli-router.py` now derives task-class CLI order from the canonical routing policy plus provider catalog instead of a second task-rule and subscription-key table, `projects/agents/src/athanor_agents/subscriptions.py` now fails closed if the canonical policy YAML is missing instead of reviving a hidden builtin policy, the fake Gemini quota model has been removed from the provider-status compatibility surface, the dashboard fixture provider surfaces now mirror the current canonical posture for Gemini, Kimi, GLM, DeepSeek, and Venice instead of replaying stale mock states, `scripts/probe_provider_usage_evidence.py` now writes live provider-specific captures that prove `deepseek_api`, `mistral_codestral_api`, and `venice_api` while classifying the remaining Vault API lanes as upstream auth failures instead of generic missing evidence, and `docs/operations/VAULT-LITELLM-AUTH-REPAIR-PACKET.md` now consolidates the remaining auth-failed lanes into one generated maintenance checklist instead of repeating generic per-provider repair prose. The newest repo-safe auth-classification tranche also teaches the generator to separate missing required env, present invalid key, and auth-mode mismatch for those Vault-backed failures, so operator next actions stop collapsing into one bogus repair instruction. |
| Operator front door | Canonical command-center URL, portal-vs-tool truth, split-brain dashboard retirement | Exactly one production command center remains, `https://athanor.local/` is live on DEV behind Caddy, DEV serves clean Next.js assets, WORKSHOP `:3001` is retired or explicitly recovery-only, and DESK resolves the canonical portal plus node-host deep links | Additional operator desktops would still need the same local alias rollout or internal DNS if they must use `*.athanor.local` | `operator-surface-registry.json` now owns human-facing surface truth, `OPERATOR-SURFACE-REPORT.md` is generated from live probes, dashboard navigation is regrouped into command-center/operate/build/domains/catalog, locked-session background mutations are short-circuited when locked, the DEV containerized runtime plus Caddy cutover is complete, the live container is standardized onto `/opt/athanor/dashboard`, `workshop-shadow-dashboard.yml` keeps the former WORKSHOP portal quarantined as a recovery-only lane rather than a live front door, the topology-owned dashboard health probe now uses lightweight `/api/operator/session` instead of timing out on `/api/overview`, active repo truth keeps machine/runtime fallbacks on `dev.athanor.local:3001` instead of raw IPs, the scripted `setup-desk-host-aliases.ps1` rollout now verifies canonical DESK resolution, and the canonical browser path plus the live runtime-fallback smoke path are both green |
| Runtime drift | Remaining stale runtime narratives and any unresolved node/model identity mismatches | Known drift items are either resolved or explicitly classified in reports | The DEV runtime repo still carries broader ownership surfaces outside implementation authority | Foundry `:8006` and Workshop `:8010` aligned in active control-plane surfaces, DESK `provider_bridge` was demoted out of live topology on 2026-03-26, and the 2026-03-29 collector now proves the retired `athanor-governor.service` listener is gone, observed `:8760` references are zero, all 9 mapped runtime-owned callers match implementation authority after the maintenance-window sync, and the last implementation-authority facade file plus its dedicated tests are deleted |
| Autonomy activation | Registry-backed post-cutover phase scope and approval boundaries | First-wave autonomy is explicit, operator-facing, validator-backed, dashboard-visible, enforced in the governor runtime, enforced upstream in autonomy-managed plan generation, and exposed as a next-phase blocker summary in live snapshots plus generated reports | Broader expansion still depends on VAULT auth repair, weak-lane verification, and tighter runtime ownership maturity | The current post-cutover activation wave now lives in `config/automation-backbone/autonomy-activation-registry.json`, `docs/runbooks/software-core-autonomy-activation.md`, and `docs/operations/AUTONOMY-ACTIVATION-REPORT.md`; software-core autonomy stays limited to coding, research, knowledge, general-assistant, and governed backlog/self-maintenance loops, runtime mutations remain approval-gated, scheduler/pipeline/auto-retry respect that phase gate, autonomy-managed plans now filter blocked agents before decomposition, workspace reactions no longer auto-wake blocked sidecar agents during the software-core phase, research jobs and cascades now submit through governed submission with autonomy provenance instead of raw task writes, manual/operator-created plans still keep the broader live roster, a shared autonomy-policy helper in `model_governance.py` now centralizes active phase facts for governor/scheduler/workplanner/workspace/plan-generator consumers, and both the live control-plane snapshots plus the generated report now expose `expanded_core_phase_2` plus its blocker ids directly |
| Secret-surface remediation | Presence-only mapping plus target delivery and approval contract | Credential surfaces are inventoried without leaking values, every reviewed live surface declares target delivery method and remediation state, and generated evidence redacts credential-bearing URLs | VAULT LiteLLM upstream provider-key delivery is only partially present at runtime, so this lane now includes the container-env repair pass in addition to general rotation posture | DEV cron now uses BASH_ENV-backed runtime.env, the reviewed Athanor systemd estate is envfile-or-envless by contract, DESK runtime env resolves Redis auth, recovery evidence redaction landed on 2026-03-26, the 2026-03-29 read-only VAULT LiteLLM env-name audit now proves the provider-key container surface is only partially populated at runtime, that audit is now persisted at `reports/truth-inventory/vault-litellm-env-audit.json` so validator, provider-report, and secret-surface-report truth all point at the same env-name artifact, `docs/operations/VAULT-LITELLM-AUTH-REPAIR-PACKET.md` now provides the generated provider-by-provider maintenance packet for the eventual ask-first VAULT repair pass, the newest repo-safe auth-classification tranche now distinguishes missing-env, invalid-present-key, and auth-mode mismatch outcomes in that packet and the provider report, and the latest owner-surface deepening now records the live launch command plus the only current `/boot/config` reference file without promoting the deployment to a managed-template classification |
| Aggressive prune | Deletion of stale startup, hardware, provider, and duplicate truth files | Superseded docs/configs/scripts are deleted or reclassified after replacement truth is verified | Remaining stale reference narratives still need a few more archive/delete passes | The second prune wave deleted stale docs under `docs/`, moved historical audit/environment/hardware/context artifacts into `docs/archive/`, and regenerated the documentation index on 2026-03-26; the current wave deleted `docs/hardware/loose-inventory.md` and `docs/hardware/CLAUDE.md`, archived `docs/BUILD-MANIFEST.md` to `docs/archive/BUILD-MANIFEST.md`, moved the historical hardware ledger to `docs/archive/hardware/hardware-inventory.md`, archived the stale Feb 14 DEV/network/node audit bundle under `docs/archive/hardware/`, archived the old deployment roadmap and removed the stale archived dependency graph, retired `scripts/validate-atlas.py`, deleted the stale atlas API/topology/UI archive docs, deleted the dead archive copies of `daily-operations`, `dev-environment`, and `complete-context-raw`, deleted the redundant short March 18 tactical plan, moved the dated dashboard audits under `docs/archive/audits/`, removed the dead dashboard `getGovernorBaseUrl()` helper, scrubbed the remaining non-archive design/research docs that still pointed at dead active build-manifest or hardware-ledger paths, rewrote `docs/MASTER-PLAN.md` so it no longer carries live-looking provider, hardware, model, or routing truth, archived `docs/BLOCKED.md`, moved the dated Feb 25 hardware deep-audit bundle under `docs/archive/hardware/`, tightened the atlas command-hierarchy reference so it no longer leaves governor durable-task ownership ambiguous, moved the old Phase 11 and Phase 16 reports out of the active `audit/` root into `docs/archive/audits/`, corrected the architect agent so it no longer describes governor durable-task truth as SQLite-backed, moved the dated rack-session plan, node1 quick-reference card, and March 14 hardware audit under `docs/archive/hardware/`, moved `ATHANOR-SYSTEM-AUDIT.md` under `docs/archive/audits/`, archived the stale dashboard spec plus the old automation-backbone provider-plan design doc and three provider-pricing research docs into explicit archive lanes, moved the old base-platform, node-role, remote-access, voice-interaction, dashboard, agent-framework, and command-center research pack under `docs/archive/research/` with ADR and design references repointed, deleted the stale `docs/research/2026-02-15-research-roadmap.md` tracker and the active `docs/design/dashboard-interactions.md` note instead of leaving misleading active-root copies behind, archived the remaining rack-session bundle plus the stale March 7/9/13/15 orchestration and optimization research notes into explicit archive lanes, rewrote `docs/research/2026-03-09-local-ai-architecture-synthesis.md` to point at the archived productivity note, rewrote `docs/TROUBLESHOOTING.md` plus `docs/REFERENCE-INDEX.md` so current provider debugging starts from catalog/report truth instead of stale provider-plan paths or raw Vault IPs, removed the old `local-system-mind` and Docker-era Open WebUI restart guidance from the active troubleshooting path, rewrote `docs/design/agent-contracts.md` plus `docs/design/intelligence-layers.md` so they no longer assign durable task, lease, or schedule ownership to governor/Athanor, updated the atlas source-reconciliation/runtime references to match that demotion, deleted the dead `.claude/plans/composed-chasing-ripple-agent-a25bacc279d82689a.md` file that still pointed at WORKSHOP dashboard and `/queue`, deleted the stale `.claude/scratch/AGENT-AUDIT-REPORT.md` audit artifact, deleted the stale `scripts/health-check-all.sh` helper, promoted `drift-check.sh` plus `run_service_contract_tests.py` in the scripts inventory, scrubbed stale stuck-queue wording from the operator helper layer, archived `docs/design/capability-codex.md`, `docs/decisions/ADR-012-litellm-routing.md`, `docs/research/2026-02-25-master-synthesis.md`, `docs/research/2026-03-08-data-sovereignty-audit.md`, `docs/research/2026-03-13-coding-models-march-update.md`, and `docs/research/2026-03-14-ai-model-landscape-q1-2026.md` so those stale provider and market-strategy narratives no longer live in active-root paths, now archives `docs/VISION.md` under `docs/archive/planning-era/VISION.md` so the historical product-intent narrative no longer sits in an active-root location, and now deletes the surviving active-root copies of `docs/architecture/complete-context-raw.md`, `docs/design/athanor-complete-context.md`, `docs/design/automation-backbone-master-plan.md`, `docs/guides/system-map.md`, `docs/guides/system-overview.md`, and `docs/projects/dashboard/SPEC.md` after repointing the remaining atlas and execution-tracker references to the archived automation-backbone plan |
| Core follow-through | Resume governor, shared-contract, Redis-auth, and import debt lanes | Truth layer is stable enough that remaining core work no longer builds on drift | Remaining shared-contract rollout and downstream durable-truth cleanup still need closure | Task create/approve/cancel/reject/review, manual task dispatch, scheduled-job manual run, governor heartbeat, emergency stop/resume, research job create/execute/delete, project milestone/advance/supervise, workforce goal create/delete, notification resolution, preference writes, explicit and implicit feedback, gallery feedback, event ingestion, pipeline steering, workspace mutation, workplan generation/redirect, convention propose/confirm/reject, subscription lease/execution/handoff mutation, manual consolidation/pattern/improvement trigger, skill create/execution/delete, core-memory update, digest generation, workflow execution, the agent-side Workshop GPU swap route, model-governance denied-path auditing, diagnosis auto-remediation, the dashboard health-summary contract, `/api/agents/proxy` gating, direct dashboard Docker mutation gating, the attention routes, the agent-output panel, `tasks.py` itself, and the signed terminal-bridge ticket plus ws-pty-bridge accepted/denied audit path now all read or persist canonical task truth without the legacy `_update_task()` wrapper; the legacy governor helper scripts in implementation authority now submit and dispatch against `/v1/tasks` instead of the retired alternate governor path; the remaining legacy governor reporting helpers, sentinel integration checks, and MCP governor snapshot bridge in implementation authority now read canonical `/v1/tasks` or `/v1/tasks/stats` instead of the retired queue/status surfaces; `runtime.subsystem.task-engine` now explicitly means durable task truth plus governor posture only, not alternate governor ownership and not any retained standalone facade implementation; the retired standalone governor facade no longer exists in implementation authority or live runtime authority, so no active repo code or service may recreate `/queue`, `/dispatch-and-run`, or `:8760` as a second task-engine surface; the DESK provider bridge now emits the shared health snapshot shape and requires a forwarded operator-action envelope plus accepted or denied audit events before it shells out to local provider CLIs; the dead governor dispatch/SQLite modules and the old archived `governor.db` snapshot are deleted from the implementation-authority tree; `tasks.py` task submit/retry event emission now uses an explicit publish helper, the hottest pending-task/status-filtered reads now consume the durable status indexes instead of rescanning the whole task hash, digest generation now reads indexed terminal statuses instead of scanning the whole task hash, `/v1/metrics/agents` now pulls per-agent task totals from canonical `get_task_stats()`, `list_tasks()` now supports canonical multi-status reads without pre-filter truncation, `owner_model` now reads complete pending+running task truth for idle-agent capacity, `list_recent_tasks()` now uses the durable updated index for recent-task views, `build_execution_run_records()` and `generate_work_plan()` no longer depend on queue-oriented pending-first slices for recent history, judge and notification pending-approval counts now come from canonical task stats instead of capped preview reads, workforce notifications now expose explicit approval preview-vs-total metadata, workplanner refill logic now uses canonical task stats instead of capped mixed samples, the primary governed task producers now all route through `submit_governed_task()` instead of duplicating governor gate plus submit plus approval-hold persistence in five different modules, completion-audit runtime subsystem census now derives from the live FastAPI route registry plus dashboard navigation/runtime-subsystem registry metadata, the completion-audit runner now uses the project-local agents pytest suite instead of false-negative unittest discovery, the stale atlas completion JSON outputs are deleted from the active tree, the atlas validator is retired, the governor router no longer duplicates the canonical task reject/batch-approve mutations, `/api/langfuse/costs` now preserves honest degraded state without same-origin `502` console fallout during dashboard audits, `/pipeline` and `/workforce` are now `live_complete` in the route census, valid header-authenticated dashboard mutations now backfill an operator envelope `session_id` when no session-id cookie is present, the command-rights registry, live `/v1/system-map`, and dashboard fixture system-map now align on governor as posture/fallback authority rather than durable-task, lease, or schedule owner, the live `/v1/governor/operations` plus `/v1/governor/operator-tests` now serve the canonical readiness and synthetic-test snapshots that the dashboard contracts already expect, the live `/v1/governor/tool-permissions` route now serves the canonical governed snapshot instead of a raw agent-metadata matrix, the dashboard promotion-control and tool-permission proxies now have direct route-level coverage, the newest dashboard tranche closed the remaining direct API wrapper coverage on `/tasks`, `/workplanner`, `/terminal`, `/monitoring`, and `/pipeline`, the full dashboard acceptance matrix is back to green at `207` test files / `352` tests, `services/quality-gate/requirements.txt` now declares the Redis dependency used by its operator-audit contract, `python scripts/run_service_contract_tests.py` now owns the disposable-venv bundle for quality-gate, gateway, governor, brain, classifier, and sentinel service verification instead of relying on ambient `pytest` state, gateway now reports canonical `internal_only` auth and topology-owned dependency ids instead of aliasing `agent_server`, `vllm_coordinator`, `vllm_worker`, and dashboard health behind legacy labels, sentinel heartbeat/readiness checks now use topology-owned service ids and health paths instead of retired `mind`, `brain`, `burn_scheduler`, `ollama_sovereign`, or `open_webui` names, active shell health helpers now use cluster-config-owned canonical labels instead of `MIND`, `Perception`, `UI`, or the old burn-scheduler wording, `contract-tests.sh` and `subscription-status.sh` now also use cluster-config-owned service URLs instead of raw IPs or `localhost`, the helper import shim no longer exports dead `GOVERNOR_URL` state and now has a real import smoke test against live cluster-config truth, the old provider-status compatibility surface now projects canonical provider posture plus direct or handoff readiness instead of a second thinner CLI-state model, the latest script-service tranche moved `subscription_burn` plus `semantic_router` onto the shared health snapshot shape, replaced mounted `cli-router` localhost calls with topology-owned URLs, required operator-action envelopes plus accepted or denied audit events for `/burn/{subscription}`, `/dispatch`, `/record-result`, and `/router/invalidate-cache`, switched the default burn working-directory fallback to the repo-owned runtime root, and added direct script-service contract coverage plus a dedicated `scripts/requirements-test.txt` lane to `run_service_contract_tests.py`; the live DEV cutover is verified, so the next governor work is downstream shared-health and durable-truth cleanup rather than more facade retirement |

## Latest Tranche

- The 2026-03-29 governor-facade cutover tranche is now verified end to end: the maintenance window backed up the runtime unit and journal, synced all 9 mapped runtime-owned callers from implementation authority into the DEV runtime repo, stopped and removed `athanor-governor.service`, and the latest collector snapshot now proves `listener_present=false`, `observed_runtime_reference_count=0`, `sync_required_count=0`, and `already_synced_count=9`.
- The operator-front-door tranche is now repo-safe complete: `config/automation-backbone/operator-surface-registry.json` plus `docs/operations/OPERATOR-SURFACE-REPORT.md` own portal and launchpad truth, dashboard IA now groups routes into command-center/operate/build/domains/catalog, the new `/catalog` launchpad is live, locked-session background mutations now short-circuit instead of hammering privileged endpoints while locked, default Ansible playbooks no longer certify WORKSHOP as a production command-center deployment path, approval-gated `ansible/playbooks/command-center-dev.yml` and `ansible/playbooks/front-door.yml` stage the DEV containerized runtime plus Caddy cutover, `ansible/playbooks/workshop-shadow-dashboard.yml` quarantines the WORKSHOP dashboard as a recovery-only shadow lane, and `scripts/setup-desk-host-aliases.ps1` now gives DESK a reproducible alias rollout for `athanor.local`, `dev.athanor.local`, `vault.athanor.local`, `interface.athanor.local`, and `core.athanor.local`.
- The follow-up live-runtime tranche rebuilt `athanor-dashboard` from the actual DEV runtime repo at `/home/shaun/repos/athanor/projects/dashboard`, which picked up the fixed chat bearer forwarding and locked-session gating path; the next repair standardized the live compose working directory and compose config onto `/opt/athanor/dashboard`, repaired the stale `/opt` env token drift, moved the collector and validator onto live compose-root truth from `docker inspect`, switched the generic dashboard service probe to lightweight `/api/operator/session`, aligned active repo truth so the human-facing front door stays `https://athanor.local/` while the runtime fallback remains the node-host alias `http://dev.athanor.local:3001`, then finished the DESK-side hostname rollout with a scripted hosts-file helper. Canonical browser verification and `python scripts/tests/live-dashboard-smoke.py` are now green against the hostname-backed operator path as well as the runtime fallback.
- The provider-execution seam is now closed repo-side: provider execution and provider-bridge inventory derive adapter truth from the provider catalog instead of a duplicated hardcoded execution table, bridge promotion now obeys catalog execution-mode policy instead of bridge advertisement alone, API-facing handoff payload timestamps are serialized consistently, the dashboard subscription execution/handoff/lease wrappers now have direct proxy-contract coverage, and the fixture `/v1/subscriptions/policy` payload now matches the live backend provider-mapping shape.
- The runtime-evidence tranche fixed the broken DEV collector transport, tightened the governor-facade evidence scan, reduced the runtime-owned caller set to `scripts/drift-check.sh`, `scripts/smoke-test.sh`, `services/cluster_config.py`, `services/governor/status_report.py`, `overnight.py`, `act_first.py`, `self_improve.py`, `services/gateway/main.py`, and `services/sentinel/checks.py`, encoded that runtime-authority lag in the repo-roots plus runtime-subsystem registries, added `config/automation-backbone/runtime-migration-registry.json`, generated `docs/operations/RUNTIME-MIGRATION-REPORT.md`, and then completed the 2026-03-29 DEV cutover so the latest collector now shows zero observed `:8760` references, zero sync-required callers, and all 9 mapped callers content-matched to implementation authority.
- The newest repo-safe migration tranche tightened the `:8760` cutover packet further: the runtime-migration registry now pins `runtime_owner_path`, `sync_strategy`, `rollback_target`, and systemd backup target by contract, the platform validator enforces those fields, the generated runtime-migration report now renders a runtime sync verification checklist with ordered caller rows plus rollback readiness, and the retirement runbook now points the operator directly at the backup root and per-caller rollback handling instead of leaving that state implicit.
- The provider-evidence tranche refreshed official verification on 2026-03-28, tightened Kimi and Z.ai pricing notes to match what the public official docs actually prove, and demoted configured Vault API lanes from "observed provider usage" to "proxy activity without provider-specific evidence" in both the generated report and the live provider snapshot path.
- The newest provider-routing tranche demoted `zai_glm_coding` from `active-routing` to `configured-unused`, updated the generated provider report to classify it as `cli_configured_without_observed_tool`, removed it from ordinary `multi_file_implementation`, `async_backlog_execution`, and `cheap_bulk_transform` routing, and taught execution-lease selection to exclude handoff-only providers from normal auto-routing unless the request explicitly opts in.
- The latest provider-confidence tranche made `routing_posture` and `routing_reason` explicit in the live routing policy, carried those fields through execution and compatibility payloads, and added direct evidence contracts to the provider catalog for Kimi, GLM, and the Vault LiteLLM API lanes so weak-lane truth is no longer encoded as prose or omission.
- The newest provider-proof tranche added `reports/truth-inventory/provider-usage-evidence.json`, `scripts/record_provider_usage_evidence.py`, and `docs/runbooks/provider-evidence-capture.md`, then taught the provider report and truth collector to surface that capture artifact so provider-specific LiteLLM proof has a concrete reviewable home instead of staying `pending` by convention.
- The newest live provider-probe tranche added `scripts/probe_provider_usage_evidence.py`, aligned the provider catalog to real served LiteLLM model ids, and recorded the first provider-specific Vault API captures: `deepseek_api`, `mistral_codestral_api`, and `venice_api` are now proven by successful completions, while `anthropic_api`, `openai_api`, `google_gemini_api`, `moonshot_api`, `dashscope_qwen_api`, `zai_api`, and `openrouter_api` are explicitly blocked on upstream auth repair.
- The newest VAULT provider-auth tranche fixed implementation-authority drift in `ansible/roles/vault-litellm/` so the tracked container env block now exports the full template-owned provider env contract, promoted `vault-litellm-container-env` to `remediation_required` after the 2026-03-29 live env-name audit, and added `docs/runbooks/vault-litellm-provider-auth-repair.md` so the remaining auth-failed lanes now have a concrete repair packet instead of generic follow-up prose.
- The follow-up 2026-03-29 read-only VAULT scan also showed there is no obvious dockerMan template or host envfile owning `litellm` by name, so the repair packet now treats the live container definition as the visible runtime authority and requires a `docker inspect litellm` backup before any live edit.
- The next 2026-03-29 read-only VAULT check proved the missing provider keys are absent from the host shell env too, so the remaining auth-failed lanes are now clearly blocked on a real secret-source update rather than another repo-side or container-only wiring fix.
- The latest VAULT packet tranche added `docs/operations/VAULT-LITELLM-AUTH-REPAIR-PACKET.md`, a generated repair packet that groups the auth-failed providers, calls out `mistral_codestral_api` as a partial contract gap even though it is already proven, prints exact missing env names from the live audit artifact, and preserves the backup plus re-probe sequence as current operator truth instead of leaving those details spread across prose.
- The follow-on owner-surface tranche deepened `scripts/vault_litellm_env_audit.py` so the cached audit now also records the live `docker/prod_entrypoint.sh --config /app/config.yaml --port 4000 --num_workers 4` launch command and the lone `/boot/config/plugins/dynamix.my.servers/configs/docker.config.json` boot-config reference, which tightens the visible runtime-authority picture without changing the current `standalone_docker_container` conclusion.
- The latest VAULT truth-layer tranche turned that env-name check into `scripts/vault_litellm_env_audit.py`, persists `reports/truth-inventory/vault-litellm-env-audit.json` on collector runs, makes the platform validator require the artifact for `vault-litellm-container-env`, and teaches both generated operations reports to print provider-specific missing env names directly so the next live repair pass can target the real gaps instead of generic auth-failure prose.
- The newest repo-safe VAULT auth-classification tranche extends that truth layer further: provider-local runtime env rules now distinguish required keys from optional compatibility keys, the generated provider report plus repair packet now separate missing required env, present invalid key, and auth-mode mismatch cases, and operator next actions now point at restore, rotate, or auth-mode review instead of one flattened repair instruction.
- The newest official-doc correction tranche tightened the weak subscription lanes further: Kimi now records membership-included pricing semantics and a 7-day rolling quota cycle from the public benefits docs, while Z.ai now records the published GLM Coding Plan floor prices (`Lite` from USD 10 and `Pro` from USD 30) plus the documented 5-hour and 7-day quota windows without pretending the subscribed tier is already proven.
- The newest runtime-cutover tranche adds `docs/operations/GOVERNOR-FACADE-CUTOVER-PACKET.md`, a generated command packet that turned the registry plus cached truth snapshot into concrete DEV backup, replace, and verify commands for the completed `:8760` maintenance window and now remains as rollback evidence instead of leaving the cutover as prose only.
- The newest autonomy-governance tranche adds `config/automation-backbone/autonomy-activation-registry.json`, `docs/runbooks/software-core-autonomy-activation.md`, and the generated `docs/operations/AUTONOMY-ACTIVATION-REPORT.md` so the post-cutover software-core activation wave, approval gates, and later expansion blockers stop living only in `STATUS.md` prose.
- The follow-up autonomy-enforcement tranche wired that contract into the governor runtime and retry path, so `scheduler`, `pipeline`, and `auto-retry` now respect the software-core phase gate instead of auto-executing only because they are operational sources.
- The newest autonomy-policy tranche centralizes active phase facts in `projects/agents/src/athanor_agents/model_governance.py`, so governor runtime, scheduler, work planner, workspace reactions, and plan generation now consume one shared policy view instead of duplicating active-state literals, enabled-agent allowlists, workload allowlists, blocked workloads, and prerequisite evaluation across five modules.
- The newest autonomy-activation checkpoint promoted `software_core_phase_1` from `ready_for_operator_enable` to active after the agents, services, dashboard, collector, and validator checkpoints all passed, while keeping `broad_autonomy_enabled=false` so first-wave autonomy is live without pretending the whole system is already unconstrained.
- The newest clean-main acceptance tranche restored the documented matrix itself: dashboard `test:e2e:terminal` now points at the real navigation spec, the full dashboard `test:e2e:audit` bundle is green on clean `main`, `gpu-orchestrator` again exposes the legacy and canonical worker-lane env aliases expected by its config contract, `ws-pty-bridge` now has a real `npm run ci` script (`node --check server.js`), and `collect_truth_inventory.py` now compares runtime-owned caller drift against `C:\Athanor` implementation authority instead of incorrectly treating the current worktree as the canonical source.
- The newest autonomy-planning tranche made autonomy-managed plans phase-aware before task submission: `plan_generator.py` now filters assigned agents against the current active phase, `decompose_plan_to_tasks()` rechecks that scope before emitting tasks, and `work_pipeline.py` now marks pipeline plan generation as autonomy-managed without constraining manual/operator plan creation.
- The newest autonomy-visibility tranche now exposes the next promotion boundary in live snapshots and dashboard fixtures: `next_phase_id`, `next_phase_status`, `next_phase_scope`, `next_phase_blocker_count`, and `next_phase_blocker_ids` are now explicit contract fields instead of implied registry-only knowledge.
- The newest autonomy-report tranche brings that same next-phase boundary into the generated operator report, so `docs/operations/AUTONOMY-ACTIVATION-REPORT.md` now carries the current phase scope, next phase, next-phase blocker count, and an explicit blocker table instead of stopping at current-phase prose.
- The newest report-freshness tranche stabilized `scripts/generate_truth_inventory_reports.py --check` under active collector churn by treating timestamp-only drift from the cached truth snapshot and VAULT env-audit artifact as non-stale, so the software-core autonomy checkpoint is now behavior-green and validator-green at the same time instead of carrying a false red doc-freshness edge.
- The newest provider-source tranche corrected weak official-source metadata for `google_gemini_api`, `moonshot_api`, `moonshot_kimi`, and `dashscope_qwen_api`, removed the legacy `official_docs_only_cost_unverified` billing-state fallback from validation and policy fixtures, and regenerated the provider report so the committed source URLs now match catalog truth.
- The newest provider-router tranche removed `scripts/cli-router.py`'s hardcoded task-to-CLI and CLI-to-subscription tables; the router now maps local task types into canonical task classes and derives CLI order from `subscription-routing-policy.yaml` plus `provider-catalog.json`, while preserving the legacy `cli` and `subscription` response shape as a compatibility shim over canonical provider truth.
- The newest prune sidecar archived `docs/research/2026-03-13-coding-models-march-update.md` and `docs/research/2026-03-14-ai-model-landscape-q1-2026.md`, removing two more live-looking provider strategy narratives from active-root paths.
- The newest prune sidecar also moved `docs/VISION.md` to `docs/archive/planning-era/VISION.md` and rewired the startup/reference layer away from the active-root copy.
- The latest prune sidecar archived `docs/research/2026-03-14-dev-tool-orchestration.md` and `docs/research/2026-03-16-local-vs-cloud-meta-orchestrator.md`, both of which still read like live provider and routing guidance even though that truth now belongs in the provider catalog, deployment registry, and generated operations reports.
- The latest provider-doc prune wave archived `docs/design/capability-codex.md`, `docs/decisions/ADR-012-litellm-routing.md`, `docs/research/2026-02-25-master-synthesis.md`, and `docs/research/2026-03-08-data-sovereignty-audit.md`, removing four more active-root narratives that still described stale LiteLLM, provider, and routing truth as if they were current.
- The compatibility-facade tranche is now fully closed: the DEV cutover is complete, the observed `:8760` reference count is zero, and the last implementation-authority facade file plus its dedicated tests are deleted, leaving only rollback evidence and migration reports.
- The newest prune wave archived the stale 2026-02-15 ADR-support research bundle into `docs/archive/research/`, including the original base-platform, network, storage, node-role, inference-engine, creative-pipeline, dashboard, agent-framework, monitoring, home-automation, media-stack, and research-roadmap docs, updated ADR-001 through ADR-011 plus the remaining live cross-references to point at those archived research documents, and removed that early Open WebUI/LangGraph/old-topology guidance from active doc paths.
- The newest provider-truth slice removed stale local monthly-cost constants and non-live GLM/Venice burn lanes from `scripts/subscription-burn.py`, so the burn scheduler now tracks only the four catalog-backed live burn lanes and gets monthly-cost truth exclusively from the provider catalog; the same tranche archived the stale `docs/design/dashboard-interactions.md` note that still described dashboard actions through LangGraph/Open WebUI-era assumptions.
- The newest prune tranche archived `docs/hardware/rack-session.md`, `docs/hardware/rack-session-full.md`, `docs/hardware/rack-session-field-manual.md`, `docs/research/2026-03-15-multi-node-docker-management.md`, `docs/research/2026-03-15-cluster-resource-optimization.md`, `docs/research/2026-03-09-local-ai-productivity-patterns.md`, `docs/research/2026-03-07-autonomous-self-improvement.md`, and `docs/research/2026-03-13-a2a-protocol-evaluation.md`, then repointed the surviving `2026-03-09-local-ai-architecture-synthesis.md` reference to the archived productivity note so active-root docs stop presenting those stale rack-layout and orchestration narratives as live guidance.

## Current Order

1. Close the remaining clean-main `projects/agents` convergence tranche so the acceptance matrix can go fully green from `main`, not just the dashboard, service-contract, GPU, and terminal-bridge lanes.
2. Keep the repo-safe VAULT auth-classification tranche current on clean `main`, including the generated repair packet, provider report, and secret-surface report, before crossing the live maintenance boundary.
3. Run the approved VAULT LiteLLM repair pass from `docs/operations/VAULT-LITELLM-AUTH-REPAIR-PACKET.md`, then re-run `scripts/vault_litellm_env_audit.py --write reports/truth-inventory/vault-litellm-env-audit.json` plus `scripts/probe_provider_usage_evidence.py --all-vault-proxy`, now that provider-specific evidence capture is automated, the env-name gap is artifact-backed, and the failures are classified as real credential/runtime gaps instead of missing probe plumbing.
4. Verify the subscribed Kimi billing tier from an operator-visible surface and restore or explicitly keep GLM CLI evidence demoted, since those are now the remaining weak non-API provider lanes.
5. Use the active software-core autonomy phase and generated report to keep first-wave autonomy bounded until the VAULT repair and remaining weak-lane verification are complete, then promote the next phase explicitly instead of by drift.
6. Keep dashboard work in sidecar mode for lower-risk breadth and keep pruning reference docs that still preserve retired standalone-governor or pre-contract script-service narratives.
7. After that, return to the next control-plane seam that still bypasses shared health/action or durable truth.
8. Keep the runbook, cutover packet, and backup root as rollback evidence only; do not recreate a standalone governor facade in implementation authority.

## Acceptance Matrix

- `python scripts/validate_platform_contract.py`
- `python scripts/generate_documentation_index.py --check`
- `python scripts/generate_project_maturity_report.py --check`
- `python scripts/generate_truth_inventory_reports.py --check`
- `python scripts/collect_truth_inventory.py`
- `python scripts/run_service_contract_tests.py`
- `cd projects/dashboard && npm test`
- `cd projects/dashboard && npm run typecheck`
- `cd projects/dashboard && npm run build`
- `cd projects/dashboard && npm run test:e2e:terminal`
- `cd projects/dashboard && npm run test:e2e:audit`
- `cd projects/agents && C:\Athanor\projects\agents\.venv\Scripts\python.exe -m pytest tests -q`
- `cd projects/gpu-orchestrator && C:\Athanor\projects\gpu-orchestrator\.venv\Scripts\python.exe -m pytest tests -q`
- `cd projects/ws-pty-bridge && npm run ci`
- `python scripts/run_contract_healer.py`
- `python scripts/generate_recovery_evidence.py`
- `bash -n scripts/drift-check.sh`
- `bash -n scripts/pre-merge-check.sh`

## Standing Rules

- Registry truth wins over stale docs and hardcoded literals.
- Runtime truth wins over historical narrative when they disagree.
- A provider or model lane is not active unless both configuration and observation support that classification.
- The DEV `:8760` cutover is complete; autonomy expansion still remains policy-scoped and approval-gated.
- The first post-cutover activation wave is software-core only.
- Runtime mutations remain approval-gated even after first activation.
- Secret values do not belong in reports or registries.
- Once replacement truth exists and is verified, stale material should be deleted rather than left in place with warning banners.


