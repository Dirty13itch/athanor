# Athanor Shared Extraction Queue

Last updated: 2026-04-06

## Purpose

Track concrete cross-repo extraction candidates that may strengthen Athanor as a platform without forcing whole-repo merges.

## Queue

| Source repo/root | Artifact or subsystem | Why reusable | Proposed destination | Extraction mode | Blockers | Validation required |
| --- | --- | --- | --- | --- | --- | --- |
| `C:\Users\Shaun\dev\athanor-next` | startup docs such as `PROJECT.md`, `docs/CODEX-NEXT-STEPS.md`, and repo continuity notes | may improve current startup and operator continuity without reviving a split-brain repo | `README.md`, `STATUS.md`, `docs/operations/*` | `copy-and-adapt` | local Athanor is behind `origin/main`; each doc must be checked against current truth before import | `python scripts/generate_documentation_index.py`; `python scripts/validate_platform_contract.py` |
| `C:\Users\Shaun\dev\athanor-next\scripts\vault-ssh.py` | VAULT SSH helper variant | no longer an active import candidate because Athanor already carries the stronger helper with topology-backed host resolution, optional runtime-env loading, and broader key-path handling | none for live code; freeze as lineage residue | `docs-only` | direct import would regress the current helper back to hardcoded host defaults and narrower key discovery | no active import planned |
| `C:\Users\Shaun\dev\athanor-next\scripts\mcp-athanor-agents.py` | older FastMCP bridge for the agent server | useful as lineage/reference only because Athanor already exposes current agent, task, model, operator, and service surfaces through the in-repo server and registry layer, while the incubation bridge hardcodes older host assumptions and bypasses current operator/governance structure | none for live code; future reference only | `docs-only` | direct import would revive a thinner external bridge contract around older node and auth assumptions instead of strengthening the current server and control-plane surfaces | no active import planned |
| `C:\Reconcile` | GitHub governance and reconciliation findings | already inventory the full Dirty13itch portfolio and clone posture | `docs/operations/ATHANOR-ECOSYSTEM-REGISTRY.md`, `docs/operations/ATHANOR-RECONCILIATION-LEDGER.md` | `docs-only` | findings must be rewritten into Athanor-owned truth and not copied blindly | `python scripts/validate_platform_contract.py` |
| `C:\Reconcile\scripts\render_ansible_template.py`, `Invoke-DeploymentDriftAudit.ps1`, and `Invoke-RepoDeploymentManifestAudit.ps1` | deployment-intent versus live-runtime audit helpers | directly useful inside Athanor because they reconcile current Ansible intent and repo deployment manifests against live node files, and Athanor already references the renderer in its own operator contract | `scripts/render_ansible_template.py`, `scripts/Invoke-DeploymentDriftAudit.ps1`, and `scripts/Invoke-RepoDeploymentManifestAudit.ps1` | `copy-and-adapt` | imported; remaining validation should confirm renderer imports and wrapper syntax stay healthy in Athanor | `python scripts/render_ansible_template.py --help`; PowerShell parser check for both audit scripts; `python scripts/validate_platform_contract.py` |
| `C:\Reconcile\scripts\Invoke-GitHubReconciliation.ps1`, `Invoke-GovernanceNormalization.ps1`, `Invoke-AthanorGuardrail.ps1`, `Invoke-GitHubWeeklyAudit.ps1`, `Invoke-PortfolioMaturityAudit.ps1`, and `Invoke-PortfolioWeeklyAudit.ps1` | March-era reconciliation and recurring wrapper scripts | useful as lineage/reference only because Athanor now owns native source discovery, preservation capture, ecosystem docs, maturity reporting, and validator-backed completion/runtime checks without depending on the old Reconcile workspace wrapper model | none for live code; future reference only | `docs-only` | direct import would revive a second control workspace and duplicate Athanor-native discovery, portfolio, and guardrail flows | no active import planned |
| `C:\Users\Shaun\dev\Local-System\deploy\dev`, `foundry`, `vault`, `workshop` | multi-node deployment lineage | strongest historical deployment topology outside current Athanor runtime packets | current runtime packets, runbooks, or archive lineage docs | `docs-only` | compare against current runtime-ownership contract first so stale deploy assumptions do not re-enter active truth | `python scripts/validate_platform_contract.py` |
| `C:\Users\Shaun\dev\Local-System\services\memory`, `mind`, and `mcp-servers` | older service and cognition boundaries | useful mainly as lineage/reference now that Athanor already uses direct agent-side Qdrant/Redis memory surfaces and has selectively ported concepts such as consolidation and CST without retaining the old six-tier service mesh or FastMCP split | lineage/reference docs only, with future concept-level extraction if a specific seam proves missing | `docs-only` | direct import would revive obsolete service boundaries and a parallel FastMCP/server topology that Athanor does not currently use | no active import planned until a narrower missing seam is identified |
| `C:\Users\Shaun\dev\Local-System\shared\python\local_system\models.py`, `workspaces.py`, and `services\mind\router.py` | broad model, workspace, and capability-routing seam | useful as lineage/reference because Athanor already carries the live GWT workspace, CST, command hierarchy, and registry-backed provider/domain control surfaces | lineage/reference docs only, with future concept-level extraction if a narrower missing seam appears | `docs-only` | direct import would reintroduce the older six-tier memory vocabulary, static workspace catalog, and heuristic capability router as parallel authority beside stronger current Athanor runtime and registry surfaces | no active import planned until a narrower missing seam is identified |
| `C:\Users\Shaun\dev\Local-System\shared` and `docs` | lineage contracts and cross-service conventions | useful for architecture history and selective contract imports | `docs/archive/lineage/` plus targeted canonical docs | `docs-only` | requires explicit distinction between history and still-live contract language | `python scripts/validate_platform_contract.py` |
| `C:\Agentic Coding Tools\src\athanor_services\route_registry` | standalone route-registry API | useful mainly as lineage evidence because Athanor already has stronger live routing surfaces in `policy_router.py`, `router.py`, and `subscription-routing-policy.yaml` | none unless later archived as lineage evidence | `docs-only` | do not import into active routing truth; active Athanor router already supersedes it | no active import planned |
| `C:\Agentic Coding Tools\src\athanor_services\event_ingest` and `core/events.py` | schema-validated control-plane event ingest service | useful as a future design reference, but not as an active code import because Athanor's current `/v1/events` route ingests external workspace items rather than enforcing a control-plane event-bus contract | lineage/reference docs or a future explicit control-plane event contract proposal | `docs-only` | direct import would blur two different event semantics and create contract confusion in the live agent server | no active import planned until Athanor formalizes a distinct control-plane event contract |
| `C:\Agentic Coding Tools\src\athanor_services\route_auditor` | route audit service | useful as lineage/reference for a future explicit route-audit subsystem, but not as an active code import because it depends on the same SQLite control-plane event database Athanor does not currently implement | lineage/reference docs only for now | `docs-only` | direct import would introduce a parallel event-storage contract instead of strengthening the current operator-audit and routing surfaces | no active import planned until Athanor formalizes a route-audit data model |
| `C:\Agentic Coding Tools\src\athanor_services\command_center_api` | standalone command-center API | useful as lineage/reference only because it is a parallel control-plane service with its own route policy, event store, dashboard overview, and provider registry rather than a missing Athanor subsystem | none for live code; future concept-level reference only | `docs-only` | direct import would introduce a second control-plane API contract alongside Athanor's current registry-backed operator, dashboard, and agent-server surfaces | no active import planned |
| `C:\Agentic Coding Tools\src\athanor_services\agent_hook\cli.py` and `sdk\client.py` | agent-side route resolution and event-emission hook | useful as lineage/reference only because it targets the older command-center API and event-bus contract rather than Athanor's current governed task, operator-envelope, and workspace/event surfaces | none for live code; future adapter design only if a narrow bridge seam is needed | `docs-only` | direct import would bind Athanor agents back to the superseded command-center API and its control-plane event schema | no active import planned |
| `C:\Agentic Coding Tools\src\athanor_services\configgen\litellm.py` and `core\litellm.py` | LiteLLM config generator from route policy | useful as lineage/reference only because Athanor already uses stronger provider-catalog, subscription-policy, validation, and VAULT auth-repair truth for LiteLLM-facing governance | none for live code; future reference only | `docs-only` | direct import would revive a parallel LiteLLM generation path derived from the old command-center route policy instead of Athanor's current registry-backed truth layer | no active import planned |
| `C:\Agentic Coding Tools\athanor-artifacts\athanor-events.schema.json` | portable control-plane event contract artifact | useful as lineage/reference material for a future explicit Athanor audit or control-plane event schema, but not as a current live contract | lineage/reference docs only for now | `docs-only` | the schema's control-plane semantics do not match the current `/v1/events` workspace-ingest route | no active import planned until Athanor defines a separate control-plane event surface |
| `C:\Agentic Coding Tools\athanor-artifacts\athanor-route-policy.yaml` | older route-policy artifact | useful as historical reference only because Athanor already has the stronger live `projects/agents/config/subscription-routing-policy.yaml` contract | none unless later archived as lineage evidence | `docs-only` | do not import into active routing truth; only archive or reference if it illuminates lineage | no active import planned |
| `C:\Users\Shaun\dev\portfolio\AI-Dev-Control-Plane` local MVP operator flow and `data\seed.json` model | local-first provider, dispatch, and result-review workflow | useful as operator-tooling lineage and dashboard-fixture reference, but not a live shared-module import because the repo is intentionally static, local-only, and sample-data driven | lineage/reference docs only, with future fixture or UX-pattern reference if needed | `docs-only` | direct import would revive a parallel sample-data operator console instead of strengthening Athanor's live registry-backed operator surfaces | no active import planned |
| `C:\Codex System Config` | `new-codex-worktree.ps1` and `audit-worktree-lanes.ps1` | directly useful for safer multi-lane repo work inside Athanor and now imported with Athanor-focused defaults | `scripts/new-codex-worktree.ps1`, `scripts/audit-worktree-lanes.ps1`, and `reports/reconciliation/worktree-lanes-latest.md` | `copy-and-adapt` | imported; the remaining Codex System Config surface is broader machine-level operator tooling, not another Athanor-core import lane | `new-codex-worktree.ps1 -WhatIf`; `audit-worktree-lanes.ps1 -WriteReport`; `python scripts/validate_platform_contract.py` |
| `C:\Codex System Config\scripts\scaffold-codex-repo.ps1`, `audit-codex-home.ps1`, `audit-core-rollout.ps1`, and repo-local templates | global Codex scaffolding and machine/repo rollout audits | useful as operator-tooling reference, but not an Athanor import because they target Codex home state and cross-repo setup rather than Athanor's own platform contracts | none for live code; operator-tooling reference only | `docs-only` | direct import would blur Athanor's repo-local operating contract with Shaun's broader machine-level Codex control plane | no active import planned |
| `C:\Users\Shaun\dev\docs\architecture-decisions.md`, `architecture-v1.1.md`, and `RECONCILIATION_SUMMARY.md` | historical Athanor and Kaizen architecture decisions | useful as lineage/reference because they preserve the older contract-driven slot model and topology assumptions, but current Athanor truth now lives in registries, current status docs, and generated reports | lineage/reference docs only | `docs-only` | direct promotion would reintroduce stale topology, sovereignty, and architecture assumptions as live truth | no active import planned |
| `C:\Users\Shaun\dev\docs\build.md`, `BUILD-MANIFEST.md`, `claude-bootstrap-guide.md`, and adjacent Claude bootstrap material | older autonomous-build and Claude startup guidance | useful only as historical operator context; current Athanor startup and continuous-completion guidance already supersede them | none for live docs; archive/reference only | `docs-only` | direct promotion would revive obsolete BUILD-MANIFEST, MEMORY, and Claude-first startup contracts that Athanor has already retired | no active import planned |
| `C:\Users\Shaun\dev\reference\model-selection-guide.md`, `AI Tools Research Notes.md`, `Comprehensive List of Agentic CLI Tools.md`, and `mcp-servers-to-add.json` | model, provider, CLI, and MCP research | useful as external research/reference only, not as active Athanor truth, because current provider/model/tool posture is already registry-backed and validator-enforced | reference only outside active truth | `docs-only` | direct import would blur current governed provider/tooling truth with speculative or date-sensitive research notes | no active import planned |
| `C:\Users\Shaun\dev\docs` and `C:\Users\Shaun\dev\reference` | remaining MCP, provider, model, and architecture research | supports better future extraction choices after narrow verification, but no broad stash import is justified | active runbooks only by narrow verified extraction; otherwise archive/reference evidence | `docs-only` | must verify against current registry truth before any promotion | `python scripts/validate_platform_contract.py` |

## Rules

- Queue an extraction only when the artifact is concretely reusable.
- Whole-repo migration is not an extraction strategy.
- Docs imports must become Athanor-owned truth or Athanor-owned archive, not a second active narrative layer.
