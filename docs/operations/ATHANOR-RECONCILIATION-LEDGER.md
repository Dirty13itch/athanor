# Athanor Reconciliation Ledger

Last updated: 2026-04-07

## Purpose

This ledger records the keep, import, archive, queue, and reject decisions made by the Athanor reconciliation program.

Current rows capture the program-foundation checkpoint. Later checkpoints should append narrower artifact-level decisions.

## Ledger

| Source | Artifact or subsystem | Disposition | Destination | Rationale | Validation completed | Checkpoint |
| --- | --- | --- | --- | --- | --- | --- |
| `C:\Athanor` | canonical implementation root | authoritative baseline retained | `C:\Athanor` | only implementation-authority root | startup docs and current registry truth reviewed | checkpoint 1 foundation |
| GitHub `Dirty13itch/athanor` | remote mainline | authoritative publication line retained | `origin/main` | local publication must align to GitHub before new push work | prior fetch and reconciliation packet evidence reviewed | checkpoint 1 foundation |
| `/home/shaun/repos/athanor` on `DEV` | runtime authority repo | runtime-governed, not imported | runtime-ownership contract and packets | runtime authority is explicit but does not override implementation truth | existing runtime reports already present | checkpoint 1 foundation |
| `C:\Users\Shaun\dev\athanor-next` | side-root commits, docs, and scripts | queued for selective import, then freeze | reconciliation packet and extraction queue | real value exists, but wholesale merge would regress authority discipline | source classified and queued | checkpoint 1 foundation |
| `C:\Users\Shaun\dev\athanor-next` | `PROJECT.md` continuity slice | imported with rewrite | `PROJECT.md` | useful startup orientation existed, but it needed rewriting to current Athanor authority and reconciliation truth | to be covered by documentation index and platform validator | checkpoint 2 continuity import |
| `C:\Users\Shaun\dev\athanor-next` | `docs/CODEX-NEXT-STEPS.md` continuity slice | imported with rewrite | `docs/CODEX-NEXT-STEPS.md` | useful Codex continuity cues existed, but they needed rebasing onto current backlog and reconciliation control docs | to be covered by documentation index and platform validator | checkpoint 2 continuity import |
| `C:\Users\Shaun\dev\athanor-next\scripts\vault-ssh.py` | rejected as active import candidate and retained as lineage residue only | none for live code | Athanor already carries the stronger helper with topology-backed host resolution, optional runtime-env loading, and broader key-path handling, so the incubation copy would be a regression | current Athanor `scripts/vault-ssh.py` and athanor-next variant reviewed side by side | checkpoint 2 continuity import |
| `C:\Users\Shaun\dev\athanor-next\scripts\mcp-athanor-agents.py` | rejected as active import candidate and retained as lineage residue only | none for live code | the script wraps the agent server as an external FastMCP bridge using older host and auth assumptions, while current Athanor already owns stronger in-repo server, service-registry, and operator-governance surfaces | incubation MCP bridge reviewed against current Athanor server and service-registry surfaces | checkpoint 2 continuity import |
| `C:\Reconcile` | GitHub portfolio registry and reconciliation findings | imported as Athanor control-doc seed | ecosystem registry, source registry, and ledger | strongest current source for full Dirty13itch repo inventory | source classified and seeded into Athanor docs | checkpoint 1 foundation |
| `C:\Reconcile` | GitHub governance baseline: 33 repos, 33 reachable clones, 0 remote-only, 0 governance drift | imported with rewrite | `docs/operations/ATHANOR-ECOSYSTEM-REGISTRY.md` and `docs/operations/ATHANOR-RECONCILIATION-PACKET.md` | still-useful portfolio and governance facts should live in Athanor-owned truth | Reconcile governance docs reviewed and folded into canonical ecosystem docs | checkpoint 2 governance import |
| `C:\Reconcile\scripts\render_ansible_template.py`, `Invoke-DeploymentDriftAudit.ps1`, and `Invoke-RepoDeploymentManifestAudit.ps1` | imported with Athanor-native adoption | `scripts/render_ansible_template.py`, `scripts/Invoke-DeploymentDriftAudit.ps1`, and `scripts/Invoke-RepoDeploymentManifestAudit.ps1` | Athanor already advertises the Ansible renderer in its operator contract and still benefits from the two targeted deployment-drift audits; these scripts reinforce current runtime/deployment verification without reviving the old Reconcile workspace model | `python scripts/render_ansible_template.py --help`; renderer smoke render against `roles/vault-litellm/templates/litellm_config.yaml.j2`; PowerShell parser checks; live `Invoke-DeploymentDriftAudit.ps1` run; live `Invoke-RepoDeploymentManifestAudit.ps1` run after remapping stale March-era source paths to current `projects/*` compose roots | checkpoint 2 reconcile tooling import |
| Athanor source truth | `ansible/host_vars/vault.yml` Qdrant Prometheus probe target | fixed to current topology truth | `ansible/host_vars/vault.yml` | the imported deployment audit exposed a stale probe target that still treated Qdrant as a node1/FOUNDRY service, while current topology, operator-surface truth, and dashboard config all place it on VAULT | deployment audit rerun after the fix; platform validator passed | checkpoint 2 deployment-drift remediation |
| Athanor Ansible deployment truth | stale `qdrant` role assignment in `ansible/playbooks/site.yml` and `ansible/playbooks/node1.yml` | removed stale node1/FOUNDRY assignment; VAULT-native role still pending | `ansible/playbooks/site.yml`, `ansible/playbooks/node1.yml`, and explanatory note in `ansible/playbooks/vault.yml` | live runtime checks prove Qdrant answers on VAULT, does not answer on FOUNDRY, and only runs as a container on VAULT, so leaving the node1 playbook assignment in place would preserve a false deployment contract | direct HTTP probes to `.203` and `.244`; live `docker ps` on VAULT and FOUNDRY; platform validator passed | checkpoint 2 deployment-authority cleanup |
| Athanor Ansible and LiteLLM source truth | stale Foundry coder lane (`qwen35-coder`) in `ansible/host_vars/core.yml` and `ansible/roles/vault-litellm/defaults/main.yml` | fixed to current runtime and registry truth | `ansible/host_vars/core.yml`, `ansible/roles/vault-litellm/defaults/main.yml`, and the LiteLLM config template comment | `model-deployment-registry.json` already classified `foundry-coder` as `devstral-small-2`, and live `docker inspect` on `vllm-coder` confirmed the dedicated Devstral bind, served model name, Mistral parser, and current sizing flags; keeping the old Qwen coder slot in repo truth would leave deployment and routing metadata internally contradictory | live `docker inspect` on `vllm-coder`; deployment drift rerun proved the remaining mismatch now lives in the runtime-owned Foundry compose file and VAULT LiteLLM config; platform validator rerun after the fix | checkpoint 2 deployment-authority cleanup |
| Athanor VAULT monitoring source truth | stale Workshop worker probe port and under-declared probe set in `ansible/host_vars/vault.yml` | fixed to current topology and operator-surface truth | `ansible/host_vars/vault.yml` | the tracked probe set was still targeting the retired Workshop `:8000` worker lane, and it was missing several already-registered specialist/internal surfaces; updating the source probe list tightens monitoring truth without mutating the runtime-owned VAULT Prometheus container directly | platform-topology review; operator-surface registry review; deployment drift audit rerun; platform validator rerun after the fix | checkpoint 2 deployment-authority cleanup |
| Athanor Workshop vLLM source truth | missing worker batching cap in `ansible/host_vars/interface.yml` | fixed to current live runtime tuning | `ansible/host_vars/interface.yml` | live `docker inspect` on `vllm-node2` shows the worker running with `--max-num-batched-tokens 2096`, and there was no competing repo-side truth for that parameter, so carrying it into host vars improves deploy fidelity without guessing over broader runtime-only drift | live `docker inspect` on `vllm-node2`; platform validator rerun after the fix | checkpoint 2 deployment-authority cleanup |
| Athanor runtime-ownership layer | explicit governed repair packets for `foundry-vllm`, `vault-litellm`, and `vault-prometheus` drift | imported as named runtime lanes and approval packets | `config/automation-backbone/runtime-ownership-contract.json` and `config/automation-backbone/runtime-ownership-packets.json` | the remaining deployment drift on those surfaces is now clearly runtime-owned, so Athanor needed first-class ownership lanes, backup roots, verification commands, and rollback contracts instead of leaving the work buried in audit prose | runtime ownership contract and packet registry extended; generated runtime ownership reports refreshed; platform validator rerun cleanly | checkpoint 2 runtime-packet deepening |
| Athanor Workshop source and ownership layer | formalized Workshop runtime root, control-surface lane, vLLM lane, grouped product lane, and ready-for-approval Workshop packets | imported as source-truth and runtime-governance deepening | `ansible/roles/dashboard/defaults/main.yml`, `ansible/roles/dashboard/tasks/main.yml`, `ansible/roles/dashboard/templates/docker-compose.yml.j2`, `config/automation-backbone/repo-roots-registry.json`, `config/automation-backbone/runtime-ownership-contract.json`, and `config/automation-backbone/runtime-ownership-packets.json` | Workshop was already a live topology node and deployment-drift host, but it was under-modeled in repo-root and runtime-ownership truth; the dashboard shadow template also failed to own the active `ws-pty-bridge` service and still certified the old `:8000` worker lane | rendered dashboard template check; deployment drift rerun; runtime ownership and repo-root reports regenerated; platform validator rerun cleanly | checkpoint 2 workshop-ownership deepening |
| `C:\Reconcile\scripts\Invoke-GitHubReconciliation.ps1`, `Invoke-GovernanceNormalization.ps1`, `Invoke-AthanorGuardrail.ps1`, `Invoke-GitHubWeeklyAudit.ps1`, `Invoke-PortfolioMaturityAudit.ps1`, and `Invoke-PortfolioWeeklyAudit.ps1` | rejected as active Athanor code import candidates and retained as lineage/reference only | none for live code; future reference only | these scripts are wrappers around the old Reconcile control workspace and would duplicate or blur Athanor's native discovery, preservation, portfolio, maturity, and validator-backed guardrail surfaces | named Reconcile wrappers reviewed against current Athanor scripts, registries, and control docs | checkpoint 2 reconcile tooling triage |
| `C:\Agentic Coding Tools` | `athanor-route-policy.yaml` | rejected as active import candidate | none; current Athanor routing contract remains canonical | Athanor already has a stronger live routing policy in `projects/agents/config/subscription-routing-policy.yaml`, so importing the older artifact would regress authority clarity | source artifact reviewed against current routing policy | checkpoint 2 extraction triage |
| `Dirty13itch/agentic-coding-tools` local clones | chose `C:\Agentic Coding Tools` as primary review root and `C:\Users\Shaun\dev\portfolio\agentic-coding-tools` as preservation duplicate | review-root decision recorded | source registry, preservation baseline, and extraction queue | both clones are on the same observed head commit, but the root-level clone carries the richer local residue and is already the active inspection root | clone states inspected side by side | checkpoint 2 clone triage |
| `C:\Agentic Coding Tools\src\athanor_services\route_registry` | rejected as active import candidate | none; current Athanor routing surfaces remain canonical | Athanor already has stronger live routing and policy surfaces, so importing the older standalone route-registry service would duplicate and weaken current truth | route-registry implementation reviewed against current Athanor router and policy router | checkpoint 2 extraction triage |
| `C:\Agentic Coding Tools\src\athanor_services\event_ingest`, `core/events.py`, and `athanor-events.schema.json` | rejected as active code import candidates and retained as lineage reference only | none for live code; future explicit event-contract design only if Athanor later formalizes a separate control-plane event surface | the extracted service models a schema-validated control-plane event bus, while Athanor's current `/v1/events` route ingests external workspace items, so direct import would conflate two different contracts | event-ingest service, schema artifact, and current Athanor events route reviewed side by side | checkpoint 2 extraction triage |
| `C:\Agentic Coding Tools\src\athanor_services\route_auditor` and `core/audit.py` | rejected as active code import candidates and retained as lineage reference only | none for live code; future route-audit design only if Athanor later formalizes a matching event-storage contract | the service depends on a SQLite `events` database with `route_class`, `boundary_decision`, and cost/runtime columns that Athanor does not currently maintain, so importing it now would create a parallel control-plane data model | route-auditor service, supporting audit logic, and current Athanor operator-audit/routing surfaces reviewed side by side | checkpoint 2 extraction triage |
| `C:\Agentic Coding Tools\src\athanor_services\command_center_api` | rejected as active code import candidate and retained as lineage reference only | none for live code; future concept-level reference only | the service is a parallel command-center/control-plane API with its own route policy, event store, dashboard overview, and provider registry, while Athanor already has stronger registry-backed dashboard, operator, and agent-server surfaces | command-center API reviewed against current Athanor operator, dashboard, and provider-governance surfaces | checkpoint 2 extraction triage |
| `C:\Agentic Coding Tools\src\athanor_services\agent_hook\cli.py` and `sdk\client.py` | rejected as active code import candidates and retained as lineage reference only | none for live code; future adapter design only if a narrow bridge seam is needed | the hook targets the older command-center API and control-plane event contract, while current Athanor agents use governed task submission, operator envelopes, and workspace/event routes instead | agent hook CLI and SDK reviewed against current Athanor task, operator, and event surfaces | checkpoint 2 extraction triage |
| `C:\Agentic Coding Tools\src\athanor_services\configgen\litellm.py` and `core\litellm.py` | rejected as active code import candidates and retained as lineage reference only | none for live code; future reference only | the generator depends on the older command-center route-policy model, while Athanor now derives LiteLLM-adjacent truth from the provider catalog, subscription routing policy, validators, and VAULT auth-repair packet | config generator reviewed against current Athanor provider, policy, and validation surfaces | checkpoint 2 extraction triage |
| `C:\Users\Shaun\dev\Local-System\services\memory`, `mind`, and `mcp-servers` | rejected as active code import candidates and retained as lineage reference only | none for live code; future concept-level extraction only if a narrower missing seam appears | Athanor already evolved toward direct agent-side Qdrant/Redis memory surfaces and selectively ported pieces like memory consolidation and CST, so importing the old six-tier service mesh or FastMCP split would regress the current architecture | Local-System memory service, MCP servers, shared event bus, and current Athanor memory/control surfaces reviewed side by side | checkpoint 2 lineage triage |
| `C:\Users\Shaun\dev\Local-System\shared\python\local_system\models.py`, `workspaces.py`, and `services\mind\router.py` | rejected as active code import candidates and retained as lineage reference only | none for live code; future concept-level extraction only if a narrower missing seam appears | Athanor already has live GWT workspace, CST, command hierarchy, and registry-backed provider/domain surfaces, so importing the older static model/workspace/router layer would create a weaker parallel authority model | Local-System shared models, workspace catalog, router heuristics, and current Athanor workspace/governance/provider surfaces reviewed side by side | checkpoint 2 lineage triage |
| `C:\Users\Shaun\dev\portfolio\AI-Dev-Control-Plane` local MVP operator flow, `app.js`, and `data\seed.json` | rejected as active code import candidate and retained as operator-tooling lineage/reference only | none for live code; future fixture or UX-pattern reference only | the repo is intentionally a local-only, sample-data static MVP, so importing it as code would create a parallel operator console rather than strengthen Athanor's live registry-backed dashboard and agent-server surfaces | README, repo-local AGENTS, product brief, app surface, and seed model reviewed against current Athanor operator/dashboard truth | checkpoint 2 shared-module triage |
| `C:\Users\Shaun\dev\Local-System` | lineage architecture and subsystem patterns | queued for lineage review | extraction queue and ecosystem registry | major predecessor line with likely reusable ideas | source classified and queued | checkpoint 1 foundation |
| `C:\Users\Shaun\dev\local-system-v4` | lineage delta docs and scripts | queued as shallow lineage reference | source registry and extraction queue | possible residual value, but lower-confidence than Local-System | source classified and queued | checkpoint 1 foundation |
| `C:\Agentic Coding Tools` | reusable route, event, audit, config, and agent-hook surfaces | queued for shared extraction review | extraction queue and ecosystem registry | strongest known shared-module candidate outside Athanor | source classified and queued | checkpoint 1 foundation |
| `C:\Users\Shaun\dev\portfolio\AI-Dev-Control-Plane` | control-plane and orchestration surfaces | queued for shared extraction review | extraction queue and ecosystem registry | likely reusable control-plane value; final role still batch-reviewed | source classified and queued | checkpoint 1 foundation |
| `C:\Codex System Config` | worktree, scaffold, and audit tooling | queued for selective operator-tooling adoption | extraction queue | useful Athanor-facing tooling may exist without making Athanor the global Codex home | source classified and queued | checkpoint 1 foundation |
| `C:\Users\Shaun\dev\docs` and `C:\Users\Shaun\dev\reference` | document and research stashes | queued as mines, not authorities | source registry and extraction queue | valuable context exists, but it must be verified before promotion | source classified and queued | checkpoint 1 foundation |
| `C:\CodexBuild\*` | prototype app roots | imported into tenant queue as future tenant candidates | tenant queue | product candidates should be tracked without merging into core | prototype roots inventoried | checkpoint 1 foundation |
| `C:\Users\Shaun\dev\quarantine\athanor` | malformed Athanor clone | rejected as source root | none | contaminated or malformed clone must never re-enter authority flow | exclusion recorded in source registry | checkpoint 1 foundation |
| mandatory non-authoritative source roots | preservation baseline | imported as Athanor evidence packet | `docs/operations/ATHANOR-RECONCILIATION-PRESERVATION-BASELINE.md` | deeper harvest work should not proceed without source-state evidence | source-root states captured from live local inspection | checkpoint 2 preservation baseline |
| mandatory source roots | machine-readable preservation capture | imported as Athanor maintenance script | `scripts/capture_reconciliation_preservation.py` and `reports/reconciliation/preservation-latest.json` | preservation should be refreshable without relying only on hand-written markdown | script added and report generation verified | checkpoint 2 preservation automation |
| `C:\` discovery surface | three-depth local source sweep | imported as Athanor maintenance script | `scripts/discover_reconciliation_sources.py` and `reports/reconciliation/discovery-latest.json` | source intake should be based on repeatable local discovery, not only remembered roots | script added; discovery report generated from live local sweep | checkpoint 2 discovery automation |
| `Dirty13itch/Field_Inspect` local family | set `C:\Field Inspect` as the primary local root and classified sibling `C:\Field Inspect-*` roots as duplicate lanes | imported into source registry and tenant queue | source registry, ecosystem registry, and tenant queue | the C drive sweep found richer live local roots than the older portfolio-clone assumption, and the family needs governed normalization | live git states and local manifests inspected across the family | checkpoint 2 tenant discovery |
| `RFI & HERS Rater Assistant` local family | set `C:\RFI & HERS Rater Assistant` as the primary local root and reclassified `C:\CodexBuild\*` variants as duplicate or successor lanes | imported into source registry and tenant queue | source registry and tenant queue | the root-level workspace carries the richest active state and should outrank the prototype copies during Batch 3 review | live git state, package manifest, docs, and dirty artifacts inspected | checkpoint 2 tenant discovery |
| `C:\Brayburn Trails HOA Website`, `C:\Users\Shaun\Ulrich Energy Auditing Website`, `C:\Meds`, and `C:\Sabrina Ulrich Counseling` | imported as governed tenant candidates from the C drive sweep | imported into source registry and tenant queue | source registry, tenant queue, and ecosystem registry where applicable | these roots are active enough to warrant Athanor governance instead of remaining accidental local side projects | live local roots inspected and recorded | checkpoint 2 tenant discovery |
| live Dirty13itch GitHub portfolio | revalidated against GitHub and folded into the ecosystem registry | imported with live drift correction | ecosystem registry and tenant queue | the March Reconcile baseline was no longer current; live GitHub now shows 35 repos, confirms `Dirty13itch/brayburn-trails-hoa-website`, and exposes `Dirty13itch/Wan2GP` as a repo without a confirmed local clone | `gh repo list Dirty13itch --limit 200` and targeted repo inspection completed | checkpoint 2 portfolio revalidation |
| live Dirty13itch GitHub portfolio | mirrored into machine-readable Athanor truth and enforcement | imported as Athanor sync tooling, registry mirror, and report artifact | `scripts/sync_github_portfolio_registry.py`, `config/automation-backbone/reconciliation-source-registry.json` `github_portfolio`, and `reports/reconciliation/github-portfolio-latest.json` | the ecosystem markdown already carried the full repo map, but the source registry did not yet mirror all 35 GitHub repos in machine-readable form; the sync seam closes that gap and gives the validator an enforceable portfolio mirror | live `gh repo list Dirty13itch --limit 200`; portfolio sync script run; validator enforcement added | checkpoint 2 portfolio mirror enforcement |
| `Dirty13itch/Wan2GP` | local clone verification | retained as remote-only portfolio repo for now | ecosystem registry, tenant queue, and `github_portfolio` mirror | targeted 3-depth `C:\` search found no repo root, only Athanor research/docs references and downloaded model files, so the repo remains governed but un-cloned locally instead of staying vague | targeted `C:\` directory and file sweep for `Wan2GP` or `wan2*`; `github-portfolio-latest.json` reviewed | checkpoint 2 portfolio mirror enforcement |
| `Field_Inspect` and `RFI & HERS Rater Assistant` local root families | tenant-family audit and preservation classification | imported as Athanor tenant-family evidence and truth tightening | `scripts/generate_tenant_family_audit.py`, `reports/reconciliation/tenant-family-audit-latest.json`, source registry, tenant queue, ecosystem registry, and reconciliation packet | the two families looked superficially similar in the source registry, but the audit proved they need different handling: `Field_Inspect` is a git-backed multi-worktree family, while `RFI & HERS Rater Assistant` currently has one dirty git-backed root plus three non-git duplicate trees | tenant-family audit script run; registry and doc surfaces updated; platform validator rerun cleanly | checkpoint 2 tenant-family normalization |
| `Field_Inspect` sibling lanes | merge-or-freeze review order narrowed from family evidence | queued in explicit order for later tenant-family review | tenant queue and tenant-family audit | branch divergence now shows `operations-runtime` (+26) and `proof-truth` (+21) as the highest-yield review lanes, while `mobile-followup` is already a freeze-first lane at +0; the proof and release lanes also now have concrete unique-script, doc, and deploy-artifact surfaces instead of vague duplicate status | `git log`, `git rev-list --left-right --count`, and tenant-family audit file-delta summary reviewed | checkpoint 2 tenant-family normalization |
| `RFI & HERS Rater Assistant` CodexBuild variants | narrowed unique-artifact review boundary | queued for artifact-only comparison before archive decision | tenant queue and tenant-family audit | the variants are not repo-backed lanes; their current distinct value is limited to config/doc deltas plus a small set of extra SQLite, migration, and feature files, so future review can stay bounded instead of treating them like full parallel projects | file-delta summary captured in tenant-family audit and reflected in tenant queue | checkpoint 2 tenant-family normalization |
| `Field_Inspect` sibling lanes | converted family-level queueing into artifact-level dispositions | imported into source registry, tenant queue, ecosystem registry, and reconciliation packet | the same control surfaces plus tenant-family evidence | direct branch replay is unsafe because both `operations-runtime` and `proof-truth` overlap the dirty shared-project workspace files already modified in the primary root; only `operations-runtime` still justifies selective replay as a product-code lane, while `proof-truth`, `proof-tooling`, and `platform-ops` now fall to lineage/reference and `mobile-followup` becomes a freeze-first lane | live branch-to-branch file diffs, overlap checks against the dirty primary root, and representative file reads across operations/runtime, proof docs, proof tooling, Serwist, and mobile API files | checkpoint 2 tenant-family disposition |
| `RFI & HERS Rater Assistant` CodexBuild variants | closed artifact-level review and downgraded variants from shadow roots to preserved archive evidence | imported into source registry, tenant queue, ecosystem registry, and reconciliation packet | the same control surfaces plus tenant-family evidence | the plain variant's extra feature files are already functionally superseded by the root workspace's current namespaced feature tree, while the safe and v2 variants only preserve scratch or migration SQLite evidence and alternate drizzle snapshots | live inspection of root and variant `PROJECT.md`, `src/features/*`, `drizzle/*.sql`, and `data/*.sqlite*` artifacts | checkpoint 2 tenant-family disposition |
| `Dirty13itch/brayburn-trails-hoa-website` and `C:\Brayburn Trails HOA Website` | locked final local product classification as `light-tenant` | imported into source registry, tenant queue, ecosystem registry, reconciliation packet, and completion program | the same control surfaces plus portfolio mirror | the workspace carries its own Next.js app, VAULT mirror, Vault secret bridge, Discourse contract, Terraform contract, and provider-flexible deploy bridge, which makes it an operator-managed product lane rather than another standalone brochure site while still keeping Athanor out of product implementation authority | live inspection of repo root, `docs/hosting-and-ownership-plan.md`, `docs/deployment-automation-runbook.md`, `docs/vault-sync-runbook.md`, `docs/vault-secret-bridge-runbook.md`, `infra/vault/README.md`, `infra/discourse/README.md`, `infra/terraform/README.md`, `scripts/common.ps1`, `scripts/deploy-vault.ps1`, `scripts/sync-to-vault.ps1`, `site/package.json`, and `netlify.toml` | checkpoint 2 tenant classification lock |
| `Dirty13itch/Local-System`, `Dirty13itch/hydra`, `Dirty13itch/kaizen`, `Dirty13itch/system-bible`, `Dirty13itch/agentic-coding-tools`, `Dirty13itch/AI-Dev-Control-Plane`, `Dirty13itch/airtight-iq-dl-forecasting-engine`, and `Dirty13itch/To-Do` | locked the remaining open Batch 1, 2, and 4 repo roles | imported into ecosystem registry, backlog, status, and portfolio mirror | the same control surfaces plus GitHub mirror | the evidence is now strong enough to close the residual role-review surface: `Local-System` stays lineage, `hydra`/`kaizen`/`system-bible` stay remote-only lineage references until clones reappear, `agentic-coding-tools` stays the only active shared-module candidate, `AI-Dev-Control-Plane` stays operator-tooling lineage/reference, and the two archive roots remain archive-only | live inspection of local roots where present, `gh repo view` on all eight repos, and missing-path checks for the three recorded `/home/shaun/repos/reference/*` lineage clones | checkpoint 2 portfolio classification closure |
| `Dirty13itch/website-app`, `Dirty13itch/Gaming-Ideas`, and `Dirty13itch/nvidia-gpu-comparison` with their local portfolio roots | locked final product classifications as `standalone-external` | imported into tenant queue, ecosystem registry, status, backlog, and portfolio mirror | the same control surfaces plus GitHub mirror | each workspace is self-contained and product-owned: `website-app` is a local-first static portfolio shell, `Gaming-Ideas` is a local project hub with product-domain Canon Vault content but no Athanor contract, and `nvidia-gpu-comparison` is a Kaizen-oriented hardware planning app without concrete Athanor runtime dependence | live inspection of each repo root, git state, README, package manifest where present, and selected supporting docs including `Gaming-Ideas/docs/SYSTEM_ARCHITECTURE.md` and `nvidia-gpu-comparison/CLAUDE.md` | checkpoint 2 tenant classification lock |
| `Dirty13itch/BKI-Tracker`, `Dirty13itch/BKI-Tracker---Calendar-reconciler`, and `Dirty13itch/Buffalog` with their local portfolio roots | locked final product classifications as `standalone-external` | imported into tenant queue, ecosystem registry, status, backlog, and portfolio mirror | the same control surfaces plus GitHub mirror | the BKI workbook repo is explicitly workbook-first, the calendar reconciler is a local Windows utility for approved calendar-to-sheet sync, and Buffalog is a local-only static tracker; none of the three present a concrete Athanor runtime or operator-tenancy contract | live inspection of each repo root, git state, README, and package/requirements manifests where present | checkpoint 2 tenant classification lock |
| `Dirty13itch/AuditForecaster` with `C:\Users\Shaun\dev\portfolio\AuditForecaster` and nested `auditforecaster` app root | locked final product classification as `standalone-external` | imported into tenant queue, ecosystem registry, status, backlog, and portfolio mirror | the same control surfaces plus GitHub mirror | the normalized root preserves product authority in a nested Next.js app with its own docs, smoke/verify lanes, and Unraid/Vercel deployment stack; that is a self-owned app surface, not an Athanor runtime tenant | live inspection of the normalized root README, nested app root, nested README, package manifest, docs index, deployment guides, and deploy scripts | checkpoint 2 tenant classification lock |
| `Dirty13itch/Favorites`, `Dirty13itch/Reverie`, and `Dirty13itch/curator-media-magic` with their local portfolio roots | locked final product classifications as `standalone-external` | imported into tenant queue, ecosystem registry, status, backlog, and portfolio mirror | the same control surfaces plus GitHub mirror | all three are self-contained product apps with local-first or static-host-ready posture; optional third-party integrations like Gemini, Plex, or Stash do not create an Athanor tenancy contract by themselves | live inspection of each repo root, git state, README, and package manifest where present | checkpoint 2 tenant classification lock |
| `Dirty13itch/buff-wrap-inspector`, `Dirty13itch/darkweb-tools-directory`, `Dirty13itch/mood-compass-whisperer`, and `Dirty13itch/reverie-dream-journal` with their local portfolio roots | locked final product classifications as `standalone-external` | imported into tenant queue, ecosystem registry, status, backlog, and portfolio mirror | the same control surfaces plus GitHub mirror | each repo is a self-contained product app with local-first or preview/static deployment posture; none showed a concrete Athanor runtime, operator dashboard, or shared-service dependency strong enough to justify tenancy | live inspection of each repo root, git state, README, and selected app/runtime files where present | checkpoint 2 tenant classification lock |
| `Dirty13itch/performer-database-app`, `Dirty13itch/stash-explorer`, `Dirty13itch/sabrina-therapy-blueprint`, and `Dirty13itch/subcontractor-snap` with their local portfolio roots | locked final product classifications as `standalone-external` | imported into tenant queue, ecosystem registry, status, backlog, and portfolio mirror | the same control surfaces plus GitHub mirror | each workspace is self-contained and product-owned: the performer and stash apps integrate with domain tools like Stash directly, while Sabrina Therapy Blueprint and Subcontractor Snap are local-first preview products without any concrete Athanor runtime or operator-tenancy contract | live inspection of each repo root, git state, README, package manifest where present, and selected deployment/runtime files such as `stash-explorer/package.json` | checkpoint 2 tenant classification lock |
| `Field_Inspect-operations-runtime` | created explicit selective replay packet | imported as canonical packet doc, generated replay report, source-registry evidence, and validator coverage | `docs/operations/FIELD-INSPECT-OPERATIONS-RUNTIME-REPLAY-PACKET.md`, `reports/reconciliation/field-inspect-operations-runtime-replay-latest.json`, source registry, completion program, and validator | the lane still carries real operations runtime product value, but its remaining use must be governed as file-bucketed replay because the dirty primary root already owns overlapping shared-project files; the packet closes the last ambiguity between “keep this branch” and “merge this branch” | live diff bucketing across replay lane files, current dirty-root overlap inspection, Field Inspect package-script inspection, replay report generation, and platform validator rerun | checkpoint 2 tenant-family execution packet |
| `C:\RFI & HERS Rater Assistant` primary root | converted the dirty repo-backed root from an implied blocker into an explicit stabilization packet and generated report | imported into tenant queue, source registry, completion narrative, and validator enforcement | `docs/operations/RFI-HERS-PRIMARY-ROOT-STABILIZATION-PACKET.md`, `reports/reconciliation/rfi-hers-primary-root-stabilization-latest.json`, and reconciliation source registry | the duplicate-tree variants were already governed, but the real remaining family blocker was the dirty primary repo itself; the new packet makes its data, script, workbook, and docs tranches explicit and validator-backed | live git status, repo-local `AGENTS.md`, `PROJECT.md`, `package.json`, dirty-file census, and validation helper path reviewed; generated report added | checkpoint 2 tenant execution governance |
| `Dirty13itch/Wan2GP` remote-only repo state | converted the remaining no-clone blocker into an explicit watch packet and generated report | imported into tenant queue, reconciliation packet, and validator enforcement | `docs/operations/WAN2GP-REMOTE-ONLY-WATCH-PACKET.md` and `reports/reconciliation/wan2gp-remote-only-watch-latest.json` | the portfolio role was already locked, but the repo still needed a governed remote-only state so it would not drift back into vague “maybe local” status | GitHub portfolio mirror reviewed; remote-only working-clone note carried into the generated watch packet | checkpoint 2 tenant execution governance |
| `C:\Codex System Config` | `new-codex-worktree.ps1` and `audit-worktree-lanes.ps1` | imported with Athanor-focused adaptation | `scripts/new-codex-worktree.ps1`, `scripts/audit-worktree-lanes.ps1`, and `reports/reconciliation/worktree-lanes-latest.md` | Athanor needed repo-native lane tooling; the imported audit also surfaced stale missing worktree residue in the current environment | `new-codex-worktree.ps1 -WhatIf`, `audit-worktree-lanes.ps1 -WriteReport`, and platform validator completed | checkpoint 2 operator-tooling import |
| `C:\Codex System Config\scripts\scaffold-codex-repo.ps1`, `audit-codex-home.ps1`, `audit-core-rollout.ps1`, and repo-local templates | rejected as active Athanor code import candidates and retained as operator-tooling reference only | none for live code; future operator reference only | these scripts and templates target Shaun's broader Codex home and cross-repo setup model, while Athanor should remain a repo-local platform instead of becoming the global Codex control-plane home | Codex System Config README, repo-local instructions, scaffolder, audit scripts, and template surfaces reviewed side by side | checkpoint 2 operator-tooling triage |
| `C:\Users\Shaun\dev\docs\architecture-decisions.md`, `architecture-v1.1.md`, and `RECONCILIATION_SUMMARY.md` | rejected as active doc import candidates and retained as lineage reference only | none for live docs; future lineage reference only | the files preserve older contract-slot, topology, and Kaizen-era architecture assumptions that are now superseded by Athanor's registry-backed truth and generated reports | named docs reviewed against current Athanor authority model and status layer | checkpoint 2 docs-stash triage |
| `C:\Users\Shaun\dev\docs\build.md`, `BUILD-MANIFEST.md`, `claude-bootstrap-guide.md`, and adjacent Claude bootstrap material | rejected as active doc import candidates and retained as historical operator context only | none for live docs; archive/reference only | these files revive obsolete BUILD-MANIFEST, MEMORY, and Claude-first startup conventions that Athanor has already retired from active truth | named docs reviewed against current startup contract and continuous-completion backlog | checkpoint 2 docs-stash triage |
| `C:\Users\Shaun\dev\reference\model-selection-guide.md`, `AI Tools Research Notes.md`, `Comprehensive List of Agentic CLI Tools.md`, and `mcp-servers-to-add.json` | rejected as active doc/config import candidates and retained as external research/reference only | none for live docs or config; reference only | they are date-sensitive research artifacts that overlap governed provider, model, and tooling truth already owned by registries and validators | named reference artifacts reviewed against current provider/tooling truth layer | checkpoint 2 reference-stash triage |
| Athanor control layer | total completion registry and canonical completion program doc | imported as governed control surface | `config/automation-backbone/completion-program-registry.json` and `docs/operations/ATHANOR-TOTAL-COMPLETION-PROGRAM.md` | the reconciliation and runtime-repair program had become broad enough that it needed an explicit machine-readable workstream/checkpoint registry plus a canonical execution doc instead of living across scattered backlog prose and chat memory | docs lifecycle updated; documentation index regenerated; platform validator extended and rerun cleanly | checkpoint 2 program-control deepening |
| `Field_Inspect-operations-runtime` replay packet | normalized from stale blocker prose to report-driven execution posture | updated canonical packet doc, replay report generator, source registry notes, tenant queue, reconciliation packet, and validator coverage | replay packet doc, replay generator, source registry, tenant queue, reconciliation packet, and validator | the control layer must derive readiness from the generated report's `execution_posture` and `overlap_count` fields while still holding shared-project and report-delivery files in a coordinated second tranche | replay generator rerun, documentation index regenerated, truth inventory reports regenerated, and platform validator rerun | checkpoint 2 packet normalization |
| `Field_Inspect-operations-runtime` safe replay tranche | materialized the governed replay lane into a dedicated review worktree and validated it | preserved as product-review execution state outside the dirty primary root | `C:\Field Inspect-ops-safe-review` plus the existing replay packet and report | the packet is no longer theoretical: the safe operations tranche now exists on `codex/replay-operations-runtime-safe` with the minimum support-set needed for contract-coherent validation, while the dirty primary root remains untouched | `npm run typecheck` and targeted `npm run test:run -- src/lib/__tests__/operations-assignment-guidance.test.ts src/lib/__tests__/operations-orchestration.test.ts src/components/dispatch/__tests__/operations-work-intake-board.test.tsx src/components/operations/today/__tests__/today-view.test.tsx src/components/operations/week/__tests__/week-view.test.tsx` passed in `C:\Field Inspect-ops-safe-review` on 2026-04-07 | checkpoint 2 tenant execution governance |
| `RFI & HERS Rater Assistant` duplicate-tree family | created explicit duplicate-evidence packet | imported as canonical packet doc, generated evidence report, source-registry evidence, and validator coverage | `docs/operations/RFI-HERS-DUPLICATE-EVIDENCE-PACKET.md`, `reports/reconciliation/rfi-hers-duplicate-evidence-packet-latest.json`, source registry, tenant queue, completion program, and validator | the root workspace remains the only repo-backed authority candidate, while the three `C:\CodexBuild\*` variants now have a bounded archive-evidence contract for SQLite and drizzle artifacts instead of lingering as prose-only shadow roots | duplicate-evidence packet generator run, documentation index regenerated, truth inventory reports regenerated, and platform validator rerun | checkpoint 2 tenant-family archive packet |
| `C:\RFI & HERS Rater Assistant` primary root | captured a preservation snapshot before later stabilization work | imported into reconciliation evidence | `reports/reconciliation/preservation-latest.json` plus the existing stabilization packet/report | the dirty repo-backed root needs preservation before tranche-by-tranche product work, especially because it has no origin remote and carries both tracked and untracked field artifacts | `python scripts/capture_reconciliation_preservation.py` completed on 2026-04-07 and now records the root's tracked/untracked surface in the shared preservation report | checkpoint 2 tenant execution governance |
| `C:\RFI & HERS Rater Assistant` stabilization review lane | materialized and validated the first four stabilization tranches in an isolated branch | preserved as product-review execution state outside the dirty primary root | `C:\RFI & HERS Rater Assistant-stabilization-review` and safe mirror `C:\CodexBuild\rfi-hers-stabilization-review` | the dirty authority candidate now has a real stabilization branch seeded with repo-contract, canonical-data, generator, and workbook tranches; Windows path issues in the spaced workspace required safe-path validation, but the branch now typechecks, passes tests, and clears the Settlers Ridge packet validator there | `npm run typecheck`, `npm run test`, and `python scripts/validate_settlers_ridge_packet.py` passed in `C:\CodexBuild\rfi-hers-stabilization-review` on 2026-04-07 after mirroring from the review worktree | checkpoint 2 tenant execution governance |
| Athanor local verification contract | made dashboard audit prerequisites deterministic and reduced bootstrap-test cycle time | imported as repo-native verification tooling and test harness | `projects/dashboard/scripts/ensure-completion-audit.mjs`, `projects/dashboard/package.json`, `projects/agents/tests/bootstrap_test_harness.py`, `projects/agents/tests/test_bootstrap_runtime.py`, and `projects/agents/tests/test_bootstrap_state.py` | the dashboard audit should no longer depend on a previous manual completion-audit run, and the bootstrap tests should not reseed a full temporary bootstrap root for every case when a seeded template can be reused safely | `node projects/dashboard/scripts/ensure-completion-audit.mjs`, `npm run pretest:e2e:audit`, `pytest tests/test_bootstrap_runtime.py -q`, and `pytest tests/test_bootstrap_state.py -q` passed on 2026-04-07; bootstrap runtime fell to about 1m43s and bootstrap state fell to about 1m20s | checkpoint 2 verification determinism |
| `C:\Field Inspect` primary root | replayed the safe operations-runtime tranche plus its minimum support set into the authority root | imported into the live tenant authority root | `C:\Field Inspect`, `docs/operations/FIELD-INSPECT-OPERATIONS-RUNTIME-REPLAY-PACKET.md`, and `reports/reconciliation/field-inspect-operations-runtime-replay-latest.json` | the replay lane is no longer only a review branch artifact; the bounded operations runtime surface now lives in the real authority root while shared-project and report-delivery follow-through stays quarantined for a later tranche | `npm run typecheck` and `npm run test:run -- src/lib/__tests__/operations-assignment-guidance.test.ts src/lib/__tests__/operations-orchestration.test.ts src/components/dispatch/__tests__/operations-work-intake-board.test.tsx src/components/operations/today/__tests__/today-view.test.tsx src/components/operations/week/__tests__/week-view.test.tsx` passed in `C:\Field Inspect` on 2026-04-07 | checkpoint 3 field inspect landing |
| `C:\RFI & HERS Rater Assistant` primary root | landed the review-branch-only stabilization code fixes into the authority root and validated through the safe-path lane | imported into the live tenant authority root | `C:\RFI & HERS Rater Assistant`, `docs/operations/RFI-HERS-PRIMARY-ROOT-STABILIZATION-PACKET.md`, and `reports/reconciliation/rfi-hers-primary-root-stabilization-latest.json` | the root already held the broader data, generator, and workbook churn; the remaining isolated delta was the validated `scripts/db-seed.ts`, `src/features/unit-types/library.ts`, and `tests/domain-validation.test.ts` slice, and the safe-path validator remains the governed way around the spaced-root Node toolchain issue | `python scripts/validate_settlers_ridge_packet.py` passed in `C:\RFI & HERS Rater Assistant` and `powershell -ExecutionPolicy Bypass -File scripts/codex/validate-safe.ps1` passed on 2026-04-07 after syncing artifacts back to the authority root | checkpoint 4 rfi stabilization landing |
| VAULT repo-owned SSH helper and env-audit lane | restored the live VAULT audit transport and regenerated the packet layer against real container evidence | imported as repo-native runtime diagnostics hardening | `scripts/vault-ssh.py`, `reports/truth-inventory/vault-litellm-env-audit.json`, `docs/operations/VAULT-LITELLM-AUTH-REPAIR-PACKET.md`, and the regenerated provider/secret/runtime ownership reports | direct OpenSSH access to VAULT was already healthy from DESK, but the Paramiko-only helper was producing a false auth failure that degraded the LiteLLM repair packet and secret-surface truth; adding native `ssh` fallback removed that false-negative control-plane bug without mutating VAULT itself | `python scripts/vault-ssh.py "hostname && date -Is"`, `python scripts/vault_litellm_env_audit.py --write reports/truth-inventory/vault-litellm-env-audit.json`, `pytest scripts/tests/test_vault_litellm_env_audit_contracts.py -q` in a pytest-capable venv, `python scripts/collect_truth_inventory.py --write reports/truth-inventory/latest.json`, sequential truth-report regeneration, and `python scripts/validate_platform_contract.py` all passed on 2026-04-07 | checkpoint 5 runtime evidence hardening |
| Live runtime reachability after maintenance-window recovery | refreshed runtime truth and removed the stale “hosts offline” assumption from the control surface | imported into generated reports and current-state docs | `reports/truth-inventory/latest.json`, `reports/deployment-drift/summary.md`, `docs/operations/RUNTIME-OWNERSHIP-REPORT.md`, `docs/operations/RUNTIME-OWNERSHIP-PACKETS.md`, and `docs/operations/TRUTH-DRIFT-REPORT.md` | VAULT, FOUNDRY, WORKSHOP, and DEV all answer from DESK again, so the remaining runtime blocker is now the approval-gated packet set itself rather than host return or another blind outage | `ssh vault "hostname && date -Is"`, `ssh foundry "hostname && date -Is"`, `ssh workshop "hostname && date -Is"`, `ssh dev "hostname && date -Is"`, `powershell -ExecutionPolicy Bypass -File .\scripts\Invoke-DeploymentDriftAudit.ps1`, sequential truth-report regeneration, and `python scripts/validate_platform_contract.py` all passed on 2026-04-07 | checkpoint 5 runtime recovery verification |
| VAULT Prometheus runtime lane | executed the governed Prometheus reconcile packet and re-probed the live service | runtime-owned config updated through the packet path | `config/automation-backbone/runtime-ownership-packets.json`, `config/automation-backbone/runtime-ownership-contract.json`, and generated runtime-ownership reports | the VAULT Prometheus lane was no longer a planning problem; the packet path was executed and the live container now answers healthy while the control plane records it as completed runtime work instead of future intent | `python scripts/vault-ssh.py "docker inspect prometheus --format '{{.State.Status}}'"`, `python scripts/vault-ssh.py "curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:9090/-/healthy"`, follow-on truth-report regeneration, and `python scripts/validate_platform_contract.py` on 2026-04-07 | checkpoint 5 runtime packet execution |
| FOUNDRY and WORKSHOP vLLM lanes | replaced floating host-local `athanor/vllm:qwen35` drift with a deterministic pinned image contract | runtime-owned compose roots reconciled and pinned | `ansible/host_vars/core.yml`, `ansible/host_vars/interface.yml`, `reports/rendered/foundry-vllm.rendered.yml`, `reports/rendered/workshop-vllm.rendered.yml`, runtime packet registry, and generated runtime-ownership reports | the same `athanor/vllm:qwen35` tag resolved to different actual builds across hosts, which made repo truth and host behavior nondeterministic; promoting the known-good Workshop lineage to pinned `athanor/vllm:qwen35-20260315` and reconciling both live compose roots closed that split cleanly | `ssh foundry "cd /opt/athanor/vllm && docker compose ps"`, `ssh foundry "curl -s http://127.0.0.1:8000/v1/models && curl -s http://127.0.0.1:8006/v1/models"`, `ssh workshop "cd /opt/athanor/vllm-node2 && docker compose ps"`, `ssh workshop "curl -s http://127.0.0.1:8010/v1/models"`, follow-on truth-report regeneration, and `python scripts/validate_platform_contract.py` on 2026-04-07 | checkpoint 5 runtime packet execution |
| Workshop control-surface lane | executed the governed reconcile packet after correcting repo truth to the live shadow-dashboard and ws-pty-bridge contract | implementation authority updated, Workshop source bundles synced, and the live shadow/control surface re-probed healthy | `ansible/roles/dashboard/defaults/main.yml`, `ansible/roles/dashboard/templates/docker-compose.yml.j2`, `projects/dashboard/.env.example`, `config/automation-backbone/runtime-ownership-packets.json`, `config/automation-backbone/runtime-ownership-contract.json`, `reports/rendered/workshop-dashboard.rendered.yml`, `/opt/athanor/dashboard/docker-compose.yml`, and `/opt/athanor/ws-pty-bridge` | the right first fix was still repo-truth narrowing, but the lane was ultimately closed by the actual 2026-04-08 backup-first maintenance pass: the Workshop shadow-dashboard and `ws-pty-bridge` source bundles were synced from implementation authority, the rendered compose artifact was installed live, and both `athanor-dashboard` and `athanor-ws-pty-bridge` came back healthy at `:3001` and `:3100` | `cd projects/agents && .\\.venv\\Scripts\\python -m pytest tests\\test_repo_contracts.py -q`, `ssh workshop "cd /opt/athanor/dashboard && docker compose up -d --build dashboard ws-pty-bridge"`, `ssh workshop "cd /opt/athanor/dashboard && docker compose ps"`, `ssh workshop "curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3100/health && curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3001/"`, `powershell -ExecutionPolicy Bypass -File .\\scripts\\Invoke-DeploymentDriftAudit.ps1`, and `python scripts/validate_platform_contract.py` on 2026-04-08 | checkpoint 5 runtime packet execution |
| DEV runtime repo mirror lane | kept the packet open after fresh reprobe showed the mirror-clean state reopened immediately after service start | runtime-governed lane remains active | `config/automation-backbone/runtime-ownership-packets.json`, `config/automation-backbone/runtime-ownership-contract.json`, and generated runtime-ownership reports | the sync path itself is proven, but the 2026-04-08 reprobe showed `athanor-overnight.service` immediately re-dirties tracked generated artifacts after a clean mirror reset, so marking the lane complete would hide real runtime authority split | `python scripts/sync_dev_runtime_repo.py`, `ssh dev "git -C /home/shaun/repos/athanor status --short"`, and follow-on truth-report regeneration on 2026-04-08 | checkpoint 5 runtime packet execution |
| `C:\Meds`, `C:\Users\Shaun\Ulrich Energy Auditing Website`, and `C:\Sabrina Ulrich Counseling` | locked standalone-external product classifications | imported into source registry, tenant queue, ecosystem registry, status, and backlog | the same control surfaces plus the GitHub portfolio mirror for GitHub-backed products | local evidence now shows all three operate as self-contained static or marketing-site products with their own deployment surfaces and no concrete Athanor dependency, so leaving them in open Batch 3 ambiguity was unnecessary drift | live git state, local manifest/readme review, hosting-config review, GitHub portfolio mirror sync, documentation index regeneration, and platform validator rerun | checkpoint 2 product classification lock |
| `Dirty13itch/savor-street-symphony`, `Dirty13itch/truck-trail-finder-app`, and `Dirty13itch/ulrich-energy-auditing` with their local portfolio roots | locked final product classifications as `standalone-external` | imported into tenant queue, ecosystem registry, status, backlog, and portfolio mirror | the same control surfaces plus GitHub mirror | the food-truck repos are local/static preview products and the Ulrich Energy repo is a local-first Python CLI workbench; none showed a concrete Athanor runtime or tenancy contract | live inspection of each repo root, git state, README, and package or project manifests where present | checkpoint 2 tenant classification lock |
| `Dirty13itch/Field_Inspect`, `Dirty13itch/Wan2GP`, and `C:\RFI & HERS Rater Assistant` | closed the last Batch 3 review rows by locking their governed roles | imported into tenant queue, ecosystem registry, status, backlog, and portfolio mirror | the same control surfaces plus existing packet/report surfaces | `Field_Inspect` is role-stable as a `light-tenant` even though its replay packet remains an open execution lane, `Wan2GP` is role-stable as remote-only `standalone-external` without a local clone, and `RFI & HERS` is role-stable as an `incubating-tenant` because its duplicate trees are already bounded by the archive-evidence packet | existing tenant-family audit, replay packet, duplicate-evidence packet, and targeted local-clone search results reviewed | checkpoint 2 tenant classification lock |
| VAULT LiteLLM owner-surface deepening | upgraded the lane from generic "secret-source missing" prose to explicit standalone-container evidence | imported into provider catalog truth, the VAULT auth-repair packet, the secret-surface report, and current-state operator docs | `config/automation-backbone/provider-catalog.json`, `reports/truth-inventory/vault-litellm-env-audit.json`, `docs/operations/VAULT-LITELLM-AUTH-REPAIR-PACKET.md`, `docs/operations/SECRET-SURFACE-REPORT.md`, and `STATUS.md` | the lane is now sharp enough to act on: there is no discovered `docker.config.json` template mapping for `litellm`, `container-watchdog` explicitly monitors the container, and historical `litellm.inspect.*.json` backups prove the live container env set itself has been the practical owner surface across multiple revisions, including a broader provider-key set on 2026-03-30 | read-only VAULT SSH probes for `docker.config.json` template mappings and backup inspect files; `python scripts/vault_litellm_env_audit.py --write reports/truth-inventory/vault-litellm-env-audit.json`; `python scripts/collect_truth_inventory.py`; truth-report regeneration; and `python scripts/validate_platform_contract.py` on 2026-04-08 | checkpoint 5 runtime owner-surface convergence |
| Foundry coder runtime truth refresh | superseded the older Devstral-era coder assumption with current live container evidence and re-aligned implementation truth to that runtime | `ansible/host_vars/core.yml`, `ansible/roles/vault-litellm/defaults/main.yml`, `config/automation-backbone/model-deployment-registry.json`, `config/automation-backbone/hardware-inventory.json`, and active reconciliation docs | direct 2026-04-08 runtime evidence now shows the running `vllm-coder` container serves `qwen3-coder-30b`, uses `qwen3_xml`, runs with `--max-num-seqs 16`, and mounts `/mnt/local-fast/models/Qwen3-Coder-30B-A3B-Instruct-AWQ:/model:ro`; the stale Devstral claim persisted simultaneously in repo truth and the host compose file, so Athanor had to realign to the running container first and leave the host-source rewrite as the remaining runtime-owned packet action | live `/v1/models` probe on `:8006`; live `docker inspect vllm-coder`; live compose-file inspection at `/opt/athanor/vllm/docker-compose.yml`; follow-on report regeneration and platform validator rerun on 2026-04-08 | checkpoint 5 runtime truth convergence |
| VAULT LiteLLM config and Anthropic provider lane | executed the config reconcile, re-probed the live container, and promoted Anthropic from `not_supported` to observed provider truth | `ansible/roles/vault-litellm/defaults/main.yml`, `ansible/roles/vault-litellm/tasks/main.yml`, `ansible/roles/vault-litellm/templates/litellm_config.yaml.j2`, `config/automation-backbone/provider-catalog.json`, `config/automation-backbone/runtime-ownership-contract.json`, `config/automation-backbone/runtime-ownership-packets.json`, and current-state operator docs | the live `/mnt/user/appdata/litellm/config.yaml` now matches implementation authority, the `litellm` container restarts healthy with Redis cache wired through the tracked env contract, and the refreshed provider-specific probe now proves `anthropic_api` through the restored `claude` compatibility lane instead of leaving the lane in stale served-model limbo | live VAULT config replacement with backup-first reprobe, `curl http://127.0.0.1:4000/health/readiness`, `python scripts/probe_provider_usage_evidence.py --all-vault-proxy --write reports/truth-inventory/provider-usage-evidence.json`, `python scripts/vault_litellm_env_audit.py --write reports/truth-inventory/vault-litellm-env-audit.json`, deployment-drift rerun, truth-report regeneration, and `python scripts/validate_platform_contract.py` on 2026-04-08 | checkpoint 5 runtime/provider convergence |

## Rules

- Add a new row whenever a source artifact becomes imported, archived, frozen, or explicitly rejected.
- Use narrow artifact or subsystem names once the program moves past source-level decisions.
- Record validation that actually ran, not what should have run later.
