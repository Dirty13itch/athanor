---
# VAULT monitoring: Prometheus + Grafana
# These run directly on Unraid via Docker API

- name: Create Prometheus config directory
  ansible.builtin.file:
    path: "{{ prometheus_config_path }}"
    state: directory
    mode: "0755"

- name: Create Blackbox config directory
  ansible.builtin.file:
    path: "{{ blackbox_config_path }}"
    state: directory
    mode: "0755"

- name: Create Grafana data directory
  ansible.builtin.file:
    path: "{{ grafana_data_path }}"
    state: directory
    mode: "0755"
    owner: "472"  # grafana user inside container
    group: "472"

- name: Deploy Prometheus config
  ansible.builtin.template:
    src: prometheus.yml.j2
    dest: "{{ prometheus_config_path }}/prometheus.yml"
    mode: "0644"
  register: prometheus_config

- name: Deploy Prometheus alert rules
  ansible.builtin.template:
    src: alert-rules.yml.j2
    dest: "{{ prometheus_config_path }}/alert-rules.yml"
    mode: "0644"
  register: alert_rules

- name: Deploy Blackbox exporter config
  ansible.builtin.template:
    src: blackbox.yml.j2
    dest: "{{ blackbox_config_path }}/blackbox.yml"
    mode: "0644"
  register: blackbox_config

- name: Run Prometheus container
  community.docker.docker_container:
    name: prometheus
    image: "{{ prometheus_image }}"
    state: started
    restart_policy: unless-stopped
    ports:
      - "{{ prometheus_port }}:9090"
    volumes:
      - "{{ prometheus_config_path }}/prometheus.yml:/etc/prometheus/prometheus.yml:ro"
      - "{{ prometheus_config_path }}/alert-rules.yml:/etc/prometheus/alert-rules.yml:ro"
      - "{{ prometheus_config_path }}/data:/prometheus"
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=90d
      - --web.enable-lifecycle
    restart: "{{ (prometheus_config.changed | default(false)) or (alert_rules.changed | default(false)) or (blackbox_config.changed | default(false)) }}"
  when: not ansible_check_mode

- name: Run Blackbox exporter container
  community.docker.docker_container:
    name: blackbox-exporter
    image: "{{ blackbox_exporter_image }}"
    state: started
    restart_policy: unless-stopped
    ports:
      - "{{ blackbox_exporter_port }}:9115"
    volumes:
      - "{{ blackbox_config_path }}/blackbox.yml:/config/blackbox.yml:ro"
    command:
      - --config.file=/config/blackbox.yml
    restart: "{{ blackbox_config.changed | default(false) }}"
  when: not ansible_check_mode

- name: Run Grafana container
  community.docker.docker_container:
    name: grafana
    image: "{{ grafana_image }}"
    state: started
    restart_policy: unless-stopped
    ports:
      - "{{ grafana_port }}:3000"
    volumes:
      - "{{ grafana_data_path }}:/var/lib/grafana"
    env:
      GF_SECURITY_ADMIN_PASSWORD: "{{ grafana_admin_password }}"
      GF_USERS_ALLOW_SIGN_UP: "false"
  when: not ansible_check_mode

- name: Wait for Prometheus to be ready
  ansible.builtin.uri:
    url: "http://localhost:{{ prometheus_port }}/-/ready"
    status_code: 200
  register: prom_ready
  retries: 5
  delay: 5
  when: not ansible_check_mode

- name: Wait for Blackbox exporter to be ready
  ansible.builtin.uri:
    url: "http://localhost:{{ blackbox_exporter_port }}/-/healthy"
    status_code: 200
  register: blackbox_ready
  retries: 5
  delay: 5
  when: not ansible_check_mode

- name: Wait for Grafana to be ready
  ansible.builtin.uri:
    url: "http://localhost:{{ grafana_port }}/api/health"
    status_code: 200
  register: grafana_ready
  retries: 5
  delay: 5
  when: not ansible_check_mode
