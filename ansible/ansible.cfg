[defaults]
inventory = inventory.yml
vault_password_file = vault-password
remote_user = athanor
private_key_file = ~/.ssh/athanor_mgmt
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml

[privilege_escalation]
become = True
become_method = sudo
become_user = root
