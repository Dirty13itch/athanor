# Reports Workspace

This directory is a compatibility shim so fresh checkouts and worktrees keep the
`projects/reports` workspace expected by the maturity registry and validators.

It is not a canonical reports root. Canonical generated truth belongs under
`C:\Athanor\reports`.

Generated completion-audit payloads stay untracked under
`projects/reports/completion-audit/`.
